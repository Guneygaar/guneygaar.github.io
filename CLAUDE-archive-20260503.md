# CLAUDE archive — entries rotated 2026-05-03

Entries below were the Last-updated stack in CLAUDE.md prior to the 2026-05-03 docs-only rotation. Preserved verbatim for history. Architecture facts current at the time of writing may have been superseded — always check live CLAUDE.md first.

Last updated: 2026-04-30 (in_production visibility by role — in_production now visible to admin/servicing/creative, hidden from client; removed blanket strip in DaySheet/Calendar.) **React (`srtd-next/src/flows/plan/hooks/usePosts.js applyRoleScope`)**: `CLIENT_ALLOWED_STAGES` drops `'in_production'`; `CREATIVE_OWN_STAGES` shrinks to `{brief, ready}` and `CREATIVE_ALL_STAGES` gains `'in_production'` (creative now sees every workspace in_production post, not just their own); `SERVICING_ALLOWED_STAGES` gains `'in_production'` unconditionally. **DaySheet (`srtd-next/src/flows/plan/sheets/DaySheet.jsx`)**: replaced the blanket `rawAllPosts.filter(p => p.stage !== 'in_production')` with role-aware strip — clients keep the filter, every other role sees in_production. **Calendar (`srtd-next/src/flows/plan/hooks/useCalendarPosts.js`)**: same role-aware swap on top of `usePosts()`. **Pipeline (`render/pipeline.js`)**: creative role gate (lines 885-897) flips `in_production` from own-only to unconditional (peer with `awaiting_approval`/`awaiting_brand_input`); servicing `_servicingStages` (lines 899-915) gains `in_production`. `_clientStages` (lines 855-861) untouched — already excludes in_production. Bundle rebuilt: `dist/sorted-react.js` / `dist/style.css`. 28 `?v=` strings bumped `20260426w -> 20260426x`.
Last updated: 2026-04-26 (Plan stability P3 — header title, date-range row, date pill iOS tap fix). **(A) TopBar title** (`Plan.jsx`): channel pills hoisted in #1077 are deleted; title now reads `Plan` (empty plan) / `Plan · LinkedIn` (single channel) / `Plan · LinkedIn, Instagram` (multi). Suffix derived from distinct `planCells.channel` via `CHANNEL_LABELS` (no workspaceChannels lookup — pills only existed when concepts existed). `CHANNEL_LABELS` (PlanView.jsx:15) flipped from short codes (`LI`/`IG`/`X`) to full names (`LinkedIn`/`Instagram`/`X`/`TikTok`/`Facebook`/`YouTube`) — sole channel display source per multi-tenant rule; only consumer was the deleted pill row + new title. Title row gets `flex:1, minWidth:0, whiteSpace:nowrap, textOverflow:ellipsis` so long multi-channel suffixes don't push the bell/menu icons. **(B) Date-range row** (`PlanView.jsx` workspace section): replaced `[<  plan.title  >] + Create plan` with three flex slots — `[spacer flex:1] [< Apr 27 – May 29 >] [Create plan, flex:1, justify-end]`. Centers the chevron+range cluster regardless of Create plan presence (clients see no Create button — cluster stays centered). New `formatPlanRange(period_start, period_end)` helper (PlanView.jsx) uses `SHORT_MONTHS` array to format `Apr 27 – May 29` (en-dash). Falls back to `plan.title || 'Untitled plan'` if dates malformed. Chevrons keep `loadAdjacentPlan('prev'|'next')` wiring. **(C) Date pill iOS tap fix**: the `:active { transform: scale(0.96) }` on `.plan-date-btn` was eating tap-end synthetic clicks on iOS Safari (touch coordinate falls outside scaled bbox between touchstart and click dispatch). Replaced with `:active { opacity: 0.7 }` (no transform). Button picks up `alignSelf:stretch` (full-row hit area), explicit `padding:'4px 6px 4px 0'`, `display:flex/flexDirection:column/alignItems:flex-start`, `WebkitTapHighlightColor:transparent`, `touchAction:manipulation`. Inner DOW/day divs marked `pointerEvents:'none'` so all touches bubble to the button. **(D) Linked cell tap → PCS**: already wired pre-PR via `handleCellTap` (PlanView.jsx:538) routing `cell_status === 'spawned' || 'linked'` + `postsByCellId[cell.id]` to `openInPcs(linkedPost, contextPosts)` (`shared/openInPcs.js`, which calls `window.SortedReact.flows.pcs.open(postId, { contextList })` with vanilla `window.openPCS` fallback). The X (remove) button at `ConceptCell` (PlanView.jsx:296) keeps `e.stopPropagation()` so X tap detaches without opening PCS. No code changes for (D); audited and confirmed intact. Bundle rebuilt: `dist/sorted-react.js` 675.38 kB / `dist/style.css` 38.23 kB. `sorted-react.js ?v=20260426r -> 20260426s` (other 27 strings stay at `p` — only the React bundle changed). Vitest: 552/552 passing.
Last updated: 2026-04-26 (Batch-3 plan management - per-cell remove, attach existing post, plan-level delete. **(A) Per-cell remove**: `ConceptCell` (PlanView.jsx:193) now wraps the cell button in a `position:relative` container and overlays a Lucide `X size=14` top-right at `opacity:0.6` (default) / `1` (hover) with a 120ms transition; click `stopPropagation` + `openRemoveCellSheet(cell.id)`. New `planSheet:'removeCell'` mounts `RemoveCellConfirm` (PlanSheets.jsx) — copy: "Remove this concept from plan?" / "Linked post stays in pipeline." Confirm calls `planStore.removeCell(cellId)` which optimistically filters the cell out, runs `deletePlanCell(cellId)` (`DELETE /plan_cells?id=eq.<id>` via apiFetch), refetches cells on success, rolls back + toasts "Failed to remove cell" on error. FK `posts.plan_cell_id REFERENCES plan_cells(id) ON DELETE SET NULL` (sql/011-plan-foundation.sql:87) keeps linked posts in the pipeline. Success toast: "Cell removed from plan." **(B) Attach existing post**: `handleAdd` (PlanView.jsx) no longer calls `addPlanCell` directly — it stores `{dateISO, channel, position}` in `planStore.pendingAdd` and opens `planSheet:'addConcept'` (`AddConceptOptionsSheet`) showing two options: `New concept` (Lucide `FilePlus` → existing addPlanCell flow) and `Attach existing post` (Lucide `Link2` → switches to `planSheet:'attachPicker'`). `AttachPostSheet` lists `listAttachablePosts()` rows (`stage IN STAGES_FOR_PLAN AND plan_cell_id IS NULL`), each row showing title, stage pill (via shared/constants STAGE_LABELS / STAGE_COLOR_VAR), target_date. Tap row → `attachExistingPost(postId)` → `callAttachExistingPost({p_plan_id, p_workspace_id, p_cell_date, p_channel, p_post_id})` (POST `/rpc/attach_existing_post_to_plan`, RPC pre-applied via Supabase MCP). On success: refetch cells, close sheet, toast "Post attached to plan."; on error: toast RPC error message verbatim. Channel taken from `pendingAdd.channel` (the original slot user tapped). **(C) Plan-level delete**: `Menu.jsx` adds a second section below `New plan` gated on `canCreatePlan && plan` — Lucide `Trash2` icon with red border + red label "Delete plan" / sub "This cannot be undone. Linked posts stay safe." Click → `closeMenu(); openDeletePlanSheet();` → `planSheet:'deletePlan'` mounts `DeletePlanConfirm`. Copy: "Delete this plan?" / "This cannot be undone. All concepts will be removed. Linked posts stay in your pipeline." Cancel + Delete buttons (Delete is red-border, Trash2 + label, busy state shows "Deleting…" with all controls locked including backdrop tap). Confirm calls `planStore.deletePlan()` → `deletePlan(plan.id)` API helper (`DELETE /plans?id=eq.<id>` via apiFetch). Cascades: plan_cells, plan_versions, plan_comments deleted via FK `ON DELETE CASCADE` (sql/011-plan-foundation.sql); posts.plan_cell_id `SET NULL` so linked posts stay in pipeline. RLS verified at sql/012-plan-spawn-rls-rpcs.sql:220 (`FOR ALL TO authenticated USING workspace_id='default'` covers DELETE). Setting `plan:null` in store re-renders `PlanView` empty state — no explicit navigation. Success toast: "Plan deleted". **API additions** (planTablesApi.js): `deletePlanCell(cellId)`, `deletePlan(planId)`, `listAttachablePosts()`, `callAttachExistingPost(args)`. **Store additions** (planStore.js): `pendingAdd`, `cellToRemove`, `attachablePosts`, `attachLoading`, `attachSubmitting`, `deletingPlan` state; `openAddConceptSheet`, `openAttachPicker`, `attachExistingPost`, `openRemoveCellSheet`, `removeCell`, `openDeletePlanSheet`, `deletePlan` actions; `closePlanSheet` extended to clear pendingAdd / cellToRemove / attachablePosts. **planSheet** enum extended with `'addConcept' | 'attachPicker' | 'removeCell' | 'deletePlan'`; Plan.jsx wires the four new mounts. Icon hygiene: Lucide-only — `X` (PlanView), `FilePlus` + `Link2` + `Trash2` (PlanSheets), `Trash2` (Menu). Bundle rebuilt: `dist/sorted-react.js` 666.75 kB / `dist/style.css` 38.23 kB. 28 `?v=` strings bumped `20260426o -> 20260426p`. Vitest: 552/552 passing.
Last updated: 2026-04-26 (PR-4 wizard fix - workspace_id resolved at runtime from profile + stuck filter widened. `planStore.createPlan` now reads `window.AppState.user.workspace_id || window.AppState.workspace.id` via internal `_resolveWorkspaceId()` helper and throws `"No workspace assigned to profile"` before calling RPC if missing - replaces hardcoded `DEFAULT_WORKSPACE_ID = 'default'` const. Same applies in `srtd-next/src/flows/plan/sheets/CreatePlanWizard.jsx` (deleted local `workspaceId` const + `|| 'default'` fallback; `createPlan(...)` no longer accepts `workspace_id` arg). Stuck-post filter widened in `planTablesApi.js listStuckPosts(periodStart)`: was `stage IN (parked, awaiting_brand_input, changes_requested) AND plan_cell_id IS NULL` (returned ~4 rows); now `stage != 'published' AND plan_cell_id IS NULL AND target_date < periodStart` (returns ~30 rows including awaiting_approval, in_production, rejected, scheduled — only published excluded since work shipped). Function now requires `periodStart` arg; `CarryoverStep.jsx` passes it through. Schema patches (RPC `create_plan_with_carryovers` reading from `profiles` not `user_roles`, `workspace_id` text→uuid cast) applied directly via Supabase MCP - no SQL in this PR. Bundle rebuilt: `dist/sorted-react.js` 653.96 kB / `dist/style.css` 38.23 kB. 28 `?v=` strings bumped `20260426j -> 20260426k`.
Last updated: 2026-04-26 (PR-4 plan creation wizard - adds 3-step bottom-sheet wizard at `srtd-next/src/flows/plan/sheets/CreatePlanWizard.jsx` covering Period (Monthly/Weekly/Custom) -> Carryovers (stuck + committed posts) -> Confirm. Stuck posts query: `stage IN (parked, awaiting_brand_input, changes_requested) AND plan_cell_id IS NULL`. Committed posts query: `stage IN (brief, in_production, awaiting_approval, scheduled) AND plan_cell_id IS NULL AND target_date BETWEEN period_start AND period_end`. Wizard mounts via `planStore.wizardOpen` flag with three entry points in `PlanView.jsx`: empty-state CTA (servicing/admin only - clients see soft "team hasn't shared a plan yet" copy with no CTA), month-nav `+ Create plan` chip, and Menu sheet `New plan` item. RPC `create_plan_with_carryovers(p_workspace_id text, p_title text, p_period_start date, p_period_end date, p_carryovers jsonb)` is SECURITY DEFINER + GRANT EXECUTE TO authenticated, server-enforces `auth.uid()` not null and role in (servicing, admin), and atomically inserts plan + plan_cells + updates `posts.plan_cell_id` + `posts.target_date` per carryover. Schema additions: `plan_cells.content_pillar TEXT`, `plan_cells.format TEXT` (read-only carry from posts in PR-4 - editable in PR-5). Carryover row: `{post_id, cell_date, channel, content_pillar, format}`. Stuck-post date suggestion = first weekday (Mon-Fri) on or after period_start; committed posts use existing `target_date` as `cell_date`. Single-tenant: workspace_id hardcoded as `'default'` const at top of files using it (TODO PR-6 for workspace-aware lookup). Channel inference: `workspace_channels` first-active by `display_order`. If 1 active, auto-fill all carryovers; if multiple, per-row dropdown. Pillar/format pills are READ-ONLY display sourced from `posts.content_pillar` / `posts.format` (no editor in wizard - PR-6 will consolidate against `workspace_pillars` / `workspace_formats`). New API helpers in `srtd-next/src/flows/plan/api/planTablesApi.js`: `listStuckPosts()`, `listCommittedPostsInRange(start, end)`, `listActiveChannels(workspaceId)`, `callCreatePlan({...})`. New planStore actions: `openWizard(presetMonth=null)`, `closeWizard()`, `createPlan(args)` - last one calls RPC, refetches plans, sets active plan to the new one, fires success toast `"{title} created : {N} posts linked"`, and closes the wizard. Bundle rebuilt: `dist/sorted-react.js` + `dist/style.css`. 28 `?v=` strings bumped `20260426h -> 20260426i`.
Last updated: 2026-04-26 (PR-2 profiles cutover — flips every code reference from `user_roles` to `profiles` (single source of truth, `id == auth.users.id`, lowercase role); flips `workspace_settings` table to renamed `workspaces` (PK `id` uuid, new `slug text UNIQUE`, slug-keyed read in `loadWorkspaceSettings`, id-keyed read in worker `checkWorkspaceEnabled`); replaces every hardcoded `'default'` workspace identifier with `AppState.workspace.id` (vanilla / React via `useAppState(s => s.workspace?.id)` or `window.AppState.workspace.id`) or `env.BOOTSTRAP_WORKSPACE_ID` (worker — added to `srtd-ai-worker/wrangler.toml [vars]`). 8 user_roles reader sites flipped: `03-auth.js resolveRoleFromToken`, `12-profiles.js fetchProfiles` cache loader (`name` → `display_name`), `06-post-create.js` owner dropdown, `render/brief.js _briefFetchTeamMembers` (collapsed dual fetch to single profiles read mapping `display_name → name`), `render/client.js _fetchClientMentionRoster` + `_displayNameForRow`, `actions/pcs.js _lookupMentionEmails` (filter `name=eq.` → `display_name=eq.`) + `_fetchPcsRoster`, `srtd-next/src/core/api/users.js listUserRoles` (collapsed dual fetch to single profiles read), `srtd-next/src/core/api/posts.js enrichPostOwner` (now reads via `posts.owner_profile_id` uuid FK with fallback to legacy `owner_user_id`). 8 hardcoded `'default'` literals replaced across `06-post-create.js`, `10-ui.js`, `actions/pcs.js`, `actions/pcs-claude-caption.js`, `srtd-next/src/shared/caption-workspace/api.js` (new `_resolveWorkspaceId(explicit)` helper), `srtd-next/src/flows/create-post/components/{GmailSheet,Header}.jsx`, `srtd-next/src/core/bridges/{gmail,claudePolish}.js`, `srtd-ai-worker/src/index.js` (lines 411/548/707/871 — fall back to `env.BOOTSTRAP_WORKSPACE_ID`). planStore.js INSERTs (627/720/731/789) verified safe — column DEFAULT now uuid; one site explicitly passes `plan.workspace_id` uuid, three omit and rely on DEFAULT. Bundle rebuilt: `dist/sorted-react.js` 632.71 kB / `dist/style.css` 38.22 kB. 28 `?v=` strings bumped `20260426g -> 20260426h`. **internal_notes RLS migration is documented post-merge SQL** (`DROP POLICY "Block clients from internal notes" ... CREATE POLICY ... USING (NOT EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'client'))`) — must be applied via Supabase MCP after merge. Worker redeployed with new `BOOTSTRAP_WORKSPACE_ID` var via GitHub Actions auto-deploy on merge. PR-3.17 (History tab replaces Activity, `post_versions` table, inline diff via `diff@^9`) and earlier entries (PR-3 plan spawn/RLS, PR-2 PlanView UI, PR-1 plan foundation, PR-3.15/16 series) archived in full to `CLAUDE-archive-20260411.md`.
