# CLAUDE.md — Sorted (srtd.io)

post_comments writes: author must be AppState.user.email, author_role must be Title-Case, attachments must follow normalizeAttachments invariant (see Composer.jsx onSend).

Last updated: 2026-04-25 (PR-3.11 PCS composer send button cleanup. Two-file change at `srtd-next/src/flows/pcs/components/Composer.jsx` (replaces the visible `Send ⌘↵` text/kbd-hint button with a single 32px circular `<ArrowUp size={16} strokeWidth={2.5} />` icon button positioned inside the composer pill on the right) plus `srtd-next/src/styles/tailwind.css` + `srtd-next/tailwind.config.js` (new token `--c-send-idle-bg` declared in BOTH `:root` (light) and `@media (prefers-color-scheme: dark) :root` blocks; bound to Tailwind class `bg-send-idle` via `colors['send-idle']`). **Idle state** (`text.trim() === '' && no attachments`): `bg-send-idle` (light `color-mix(in srgb, #000000 8%, transparent)` / dark `color-mix(in srgb, #FFFFFF 10%, transparent)`) + `text-text-mid` arrow color (light `#4A4640` / dark `#B1ADA1`). **Active state** (any content): `bg-terracotta` (light `#B85430` / dark `#C15F3C`) + `text-white` (`#FFFFFF` paints on brand surface, not UI surface — same exception class as carousel dot tokens). **Sending state**: `opacity-50 pointer-events-none` overlay on the active visual; the `disabled={sending}` attr also blocks clicks. **Transition**: `transition-colors duration-150` so the idle→active color swap is smooth as the user types. **No `Send` text label, no `⌘↵` keyboard hint, no tooltip.** The button shape is `w-8 h-8 mr-1 rounded-pill` (32px circle, 4px right margin inside the composer strip). **Input behaviour**: `enterKeyHint="send"` added to the textarea so iOS keyboard relabels the Return key. `onKeyDown` rewritten — Enter (with or without Shift) calls `e.preventDefault()` then fires `onSend()` when there is content and no in-flight send. The legacy `(metaKey||ctrlKey)+Enter` branch is gone — single Enter now sends, matching Anthropic-style chat-pill UX (comments are single-line in practice; multi-line paste still grows the textarea via the existing `useEffect([text])` height-clamp at 200px). **Vanilla composer is UNTOUCHED** — `render/client.js` already shipped a `SEND_SVG` arrow icon with `aria-label="Send"` and no `Send`/`⌘↵` label (line 802), so no parity changes were needed there. **Reuse-only**: `ArrowUp` from `lucide-react` (added to existing `lucide-react` import line), the existing `onSend` function and its mention-to-email resolution + optimistic-insert path, the `+` button + sheet, the textarea ref, the mention picker, and every other composer behaviour are unchanged. **PR-C1 dual-mode parity verified**: idle bg auto-flips light/dark via the `prefers-color-scheme` cascade in `tailwind.css`; active bg references `var(--c-terracotta)` which already auto-flips; arrow `text-text-mid` (idle) and `text-white` (active) both resolve correctly in both modes. No hardcoded `#FFFFFF` / `rgba(0,0,0,...)` in the JSX — `text-white` is Tailwind's built-in default and is acceptable here per the same exception that allows `--c-dot-active: #FFFFFF` (paints on brand/photo, not on a UI surface). 28 `?v=` strings bumped `20260425n → 20260425o`. React bundle rebuilt at `dist/sorted-react.js` 562.08 kB; `dist/style.css` 34.81 kB; `window.SortedReact.version` bumped `0.9.22 → 0.9.23`. No new deps, no Worker changes, no SQL changes, no test changes. **PCS composer invariants: (a) send button is a 32px circular icon button (no text label, no kbd hint, no tooltip); (b) idle state = `bg-send-idle` + `text-text-mid`; active state = `bg-terracotta` + `text-white`; sending state = active visual + `opacity-50 pointer-events-none`; (c) Enter sends (matches `enterKeyHint="send"`), no `⌘+Enter` shortcut, no Shift+Enter newline; (d) the `+` button, mention picker, polish button, and every other composer affordance are unchanged.**) Prior (2026-04-25) was (PR-3.10 PCS post-detail top-bar + carousel cleanup. Three-file change at `srtd-next/src/flows/pcs/components/KickerRow.jsx`, `srtd-next/src/flows/pcs/components/PhotoStrip.jsx`, and `srtd-next/src/styles/tailwind.css`, plus deletion of `srtd-next/src/flows/pcs/components/PcsKebabMenu.jsx`. **Top bar final state**: X | status pill | Copy icon | sidebar (PanelRight) icon. The agency stage-action kebab (Recall, Nudge client, Reschedule, Publish now, Assign, Rework brief, Send for approval, Revive) is REMOVED entirely from the post-detail top bar — `PcsKebabMenu.jsx` deleted, `KickerRow.jsx` import + mount block dropped, unused `actor` prop removed from the KickerRow signature and the `<KickerRow .../>` mount in `PCS.jsx`. **Recall and Nudge client are dropped with no replacement** — the `activity_log 'recalled'` audit label that the kebab wrote alongside stage moves goes with it. Reschedule / Publish now / Assign / Rework brief / Send for approval / Revive are all reachable through the existing PcsDetailSheet (the sidebar icon on the right of the top bar) and through stage transitions, so no replacement surface is needed. Vanilla `actions/pcs.js` is untouched and still ships the full kebab for vanilla PCS sessions. **Post-detail carousel** in `PhotoStrip.jsx`: the in-image `{i+1} / {count}` counter pill and the `<Maximize2>` expand button (top-right cluster) are deleted. Tap-on-image still opens the lightbox via the existing `openLightbox(i)` handler — no change to the lightbox component, no change to `PhotoKebabMenu.jsx`. The agency-only photo kebab still renders inside the cluster when `canEdit && !isClient`. **New CarouselDots overlay** mounts absolute-positioned at the middle-bottom of the carousel viewport (centered horizontally, 14 px from the bottom). Render rules: `count <= 1` returns null (single image = no dots). `count <= 5` renders `count` dots. `count > 5` renders 5 dots with iOS-Photos-style center-shift windowing — the active dot stays in the middle visible slot when possible (`start = current - floor(window/2)`, clamped to `[0, count - window]`), and edge dots that sit outside the visible window shrink to 4-px circles to indicate "more dots beyond this edge". Active dot: 18-px wide, 6-px tall, 3-px border-radius pill, `bg: var(--c-dot-active)`. Idle dot: 6-px circle, `bg: var(--c-dot-idle)`. Width/height/radius transition 180 ms ease so the active pill grows smoothly as the user swipes. **4-second fade**: `useState dotsActive` + `useRef fadeTimer`. Mount + every `scrollIdx` change + every `post.post_id` change clears the timer, sets `dotsActive=true`, schedules a 4 s `setTimeout` that flips it to `false`. The carousel container's `onTouchStart` calls `pingDots()` to restore + restart. Container `opacity` transitions between 1 and 0.4 with `transition: opacity 200ms ease`. Cleanup on post navigation guarantees no orphan timers across PCS prev/next. The dots container is `pointer-events:none` (decorative, not interactive) and carries `aria-hidden="true"`. **Drop shadow**: `filter: drop-shadow(0 1px 2px var(--c-dot-shadow))` on the dots container so the shadow paints around each dot's silhouette and stays legible on bright photos. `z-index: 4` keeps the dots above the image but below the kebab cluster (top-right) and the lightbox layer (z 1600). **New tokens** in `tailwind.css`, declared IDENTICALLY in both `:root` (light) and `@media (prefers-color-scheme: dark) :root` blocks because the dots paint over the photo, not over a UI surface, so they should not flip with theme: `--c-dot-idle: #FFFFFF`, `--c-dot-active: #FFFFFF`, `--c-dot-shadow: rgba(0,0,0,0.30)`. The `rgba()` here is a deliberate exception to the no-rgba rule because it is the only way to express partial transparency inside a `drop-shadow()` filter, and the shadow is a photo-overlay primitive, not a UI surface. **Right-cluster spacing**: with the kebab gone the right cluster is now `Copy | PanelRight` with the existing `gap-0.5` between them and `pr-2` on the cluster — visually balanced against the left cluster's `X | (prev/next when context list)` in both light and dark. Verified `flex-1 min-w-0` on the title cluster keeps it centered with the new shorter right cluster. **Bottom action row** (Copy / Edit / AI / Proofread) is OUT OF SCOPE and untouched. **Vanilla** (`render/*.js`, `10-ui.js`, `actions/pcs.js`) is OUT OF SCOPE and untouched. 28 `?v=` strings bumped `20260425m → 20260425n`. React bundle rebuilt at `dist/sorted-react.js` 562.01 kB; `dist/style.css` 34.55 kB; `window.SortedReact.version` bumped `0.9.21 → 0.9.22`. No new deps, no Worker changes, no SQL changes, no test changes. **PCS post-detail invariants: (a) top bar = X / status / Copy / sidebar — kebab REMOVED, no Recall, no Nudge client, no replacement; (b) carousel overlay = dots only — no counter pill, no expand button — tap image opens lightbox; (c) dots render N for N images (1 image = none, 2-5 = N, 6+ = 5 with iOS center-shift), white in both modes, 4 s fade-to-40% with restore on touchstart/scroll/swipe/post-nav; (d) `PcsKebabMenu.jsx` is DELETED — do not reintroduce.**) Prior (2026-04-25) was (PR-3.9 PCS inline caption edit + Copy icon. Two-file change at `srtd-next/src/flows/pcs/components/CaptionBlock.jsx` and `srtd-next/src/flows/pcs/components/KickerRow.jsx` plus a new monotonic counter slot in `srtd-next/src/flows/pcs/pcsStore.js`. **Caption commits go through `useOptimisticPatch`**: the prior bespoke `patchPost` + `writeAudit` retry inside `applyCaption` is replaced with `commit('caption', next, { auditField:'caption', actor:userEmail, label:'Caption' })`, then `reseedOgPreview(post.post_id)` on resolve — mirrors the PR-3.8 title pattern (5 s undo toast, RETRY toast on failure, hook does NOT auto-reopen the input on retry, and the hook does not reseed OG so the manual reseed is required). Empty save (`!trimmed`) exits silently with NO patch; unchanged value (`trimmed === caption`) also exits silently. **Long-press-to-edit on the caption body is REMOVED** — `startLP` / `cancelLP` and every `onTouchStart`/`onMouseDown`/`onContextMenu` body wiring is gone, restoring native iOS text-select on the caption body. The pencil icon at the bottom STAYS as the primary edit trigger. **Edit-mode textarea**: border switched from `border-border-warm` to inline `border:1px solid var(--c-terracotta)` (matches PR-3.8 title). Auto-grow via `useLayoutEffect` on `editing`+`draft` deps — sets `style.height='auto'` then `Math.min(scrollHeight, 400)+'px'`; `maxHeight:400px` + `overflowY:auto` so anything taller than 400 px scrolls inside the textarea. **Word counter is 3-state** mirroring `CaptionField.jsx:54-60` Create Post: `wordStateOf(n)` returns `'default'` (n=0), `'green'` (80-100), `'amber'` (101-125), `'red'` (126+). Default→`text-text-dim` (light `#8A857A` / dark `#6A655C`), green→inline `#7DBE8A` (matches `tokens.good` from `core/tokens.js` since no `text-good` Tailwind class exists), amber→`text-amber` (light `#C8841A` / dark `#F6A623`, both already declared in `tailwind.css :root` and `@media (prefers-color-scheme: dark)`), red→`text-red` (light `#D83838` / dark `#FF4B4B`). Both display + edit counters use the same mapping. **Copy icon in KickerRow** sits LEFT of the kebab in the right cluster (order: …`prev`/`next` … | Copy | kebab | PanelRight). Lucide `<Copy size={16} strokeWidth={1.75} />`, canonical `w-9 h-9 rounded-sm2 text-text-mid hover:text-text-loud active:bg-bg-2 active:scale-[0.96]` button shape, `aria-label="Copy caption"`. Visible to ALL roles (no `canEdit` gate on the button itself). Tap = `navigator.clipboard.writeText(post.caption || '')` with hidden-textarea + `document.execCommand('copy')` fallback; toasts `'Copied'` unconditionally on the resolved/rejected path (empty caption still toasts). Long-press (500 ms) = enter caption edit mode, ONLY when `canEdit` (Client + Creative get tap-only behaviour because `useLongPress({ enabled: canEdit })` returns `{}` and adds zero handlers). `onContextMenu={e=>e.preventDefault()}` suppresses the iOS Safari long-press menu collision. `useLongPress` already vibrates 10 ms on long-press fire (Vibration API gated). **Edit trigger plumbing** uses a monotonic counter, not a ref/imperative handle: `pcsStore.captionEditRequested:number` + `requestCaptionEdit:()=>set(s=>({captionEditRequested:s.captionEditRequested+1}))`. KickerRow Copy long-press calls `usePcsStore.getState().requestCaptionEdit()`. `CaptionBlock` subscribes to the counter, tracks the last-seen value via `useRef`, and on increment seeds `draft` from `post.caption` and flips `editing=true` (gated on `canEdit`). Counter survives post navigation without spuriously triggering edit because the ref initialises to the current counter value on mount. **PCS.jsx** threads `canEdit` through to `<KickerRow canEdit={canEdit} ... />`. Vanilla `actions/pcs.js`, `render/*.js`, `10-ui.js` — NO CHANGES. **Dual-mode parity verified** in light + dark via tailwind.css token cascade: caption body `text-text-loud`, edit-mode bg `bg-bg-draft` (light `#F7EDE0` / dark `#201810`), edit-mode border `var(--c-terracotta)`, Cancel `text-text-soft → text-text-loud`, Save `bg-terracotta-grad` + `text-text-loud`. No hardcoded `#FFFFFF` / `rgba(0,0,0,...)` anywhere. 28 `?v=` strings bumped `20260425l → 20260425m`. React bundle rebuilt at `dist/sorted-react.js` 569.18 kB; `dist/style.css` 34.07 kB; `window.SortedReact.version` bumped `0.9.20 → 0.9.21`. No new deps, no Worker changes, no SQL changes. **PCS caption edit invariants: (a) caption commits go through `useOptimisticPatch.commit('caption', ...)` followed by manual `reseedOgPreview(post.post_id)`; (b) Copy icon in post detail top bar — tap to copy caption body verbatim, long-press to enter caption edit when `canEdit`; (c) long-press-to-edit on caption body has been REMOVED — body is native text-selectable; (d) word counter is 3-state matching `CaptionField.jsx`: default / green 80-100 / amber 101-125 / red 126+.**) Prior (2026-04-25) was (PR-3.8 PCS inline title edit. Single-file change at `srtd-next/src/flows/pcs/PCS.jsx`. Replaces the full-screen TITLE editor with inline tap-to-edit on the post detail h1. Deleted `TITLE_FIELD_CONFIG` constant and the `{activeSheet === 'title' && <PropertySheet .../>}` mount; the `activeSheet` state slot is preserved per spec but is currently unused inside PCS.jsx (every other field already lives in `PcsDetailSheet`). New local state: `editingTitle` (bool) + `titleDraft` (string). Display mode renders the same `font-serif text-[26px] font-semibold leading-[1.15] tracking-[-0.02em] text-text-loud` h1; when `canEdit` (i.e. `!isClient`), tap seeds `titleDraft` from `post.title` and flips into edit mode with `hover:bg-bg-2` affordance. Edit mode renders a transparent `<input type="text" maxLength={200} autoFocus enterKeyHint="done">` matching the h1 typography exactly, with `border:1px solid var(--c-terracotta)` (light `#B85430` / dark `#C15F3C` — visible on both palettes), `outline:none`, `boxShadow:none`, `caretColor:currentColor`. Negative-margin `-mx-2 px-2 -my-1 py-1` mirrored on both display and edit so layout never shifts. Enter and blur both commit; Escape discards silently. `commitTitle()` trims `titleDraft`; if empty OR equal to `post.title`, exits silently with NO patch. Otherwise calls `commit('title', trimmed, { auditField:'title', actor: userEmail, label: 'Title' })` via the existing `useOptimisticPatch` hook (5 s undo toast, RETRY toast on failure — the hook does NOT auto-reopen the input on retry, the toast replays the patch silently), then fires `reseedOgPreview(post.post_id)` because the hook does not reseed OG (mirrors `PropertySheet.jsx:28`). RLS verified via Supabase MCP: `posts` has a blanket public `polcmd '*'` policy that supersedes role-based policies; the frontend `canEdit = !isClient && !isCreative` is the real gate, identical to today's full-screen TITLE flow — no regression. Vanilla `actions/pcs.js _pcsTitleEdit` (lines 1156-1187) untouched; vanilla still ships with its own title flow. Verified visual parity in light + dark: `text-text-loud` (#1A1816 light / #F4F3EE dark), transparent input bg inherits page bg, terracotta border visible in both, `hover:bg-bg-2` (#F4F0E8 light / #201E1A dark) subtle in both, caret legible in both. No hardcoded `#FFFFFF` / `rgba(0,0,0,...)` anywhere — all token-based per PR-C1 rule. 28 `?v=` strings bumped `20260425k → 20260425l`. React bundle rebuilt at `dist/sorted-react.js` 568.18 kB; `dist/style.css` 34.10 kB; `window.SortedReact.version` bumped `0.9.19 → 0.9.20`. No new deps, no Worker changes, no SQL changes. **PCS title edit invariant: title edits are now inline only; the full-screen TITLE PropertySheet mount has been removed. Tap the h1 (gated by `canEdit`) to enter edit mode; Enter/blur commit, Escape discards silently. Empty or unchanged values exit without a patch. Successful commit triggers `reseedOgPreview` manually because `useOptimisticPatch` does not reseed.**) Prior (2026-04-25) was (Plan React token isolation per theme attribute. Single-file change at `srtd-next/src/flows/plan/tokens.css`. Root cause of the residual PR-C1 light-mode breakage (BLACK stage header bands, owner badges painting wrong color) was the cascade — Plan's `[data-plan-root][data-plan-theme="light"]` block only declared a SUBSET of tokens (pillars + `--backdrop-tint` + `--shadow-fab`); every inherited token (`--c-bg-2`, `--c-divider-warm`, `--c-text-soft`, `--c-role-creative`, `--c-role-client`, etc.) fell through to `srtd-next/src/styles/tailwind.css :root`, which then got globally overridden by `@media (prefers-color-scheme: dark) :root` whenever the user's OS preference disagreed with the resolved Plan theme — same specificity, latter rule wins, dark values bled into the light Plan tree. **Fix**: rewrote tokens.css so `[data-plan-root]`, `[data-plan-root][data-plan-theme="light"]`, the `@media(prefers-color-scheme: dark) [data-plan-root]:not([data-plan-theme="light"])` block, AND `[data-plan-root][data-plan-theme="dark"]` each redeclare EVERY token Plan consumes (37 tokens covering surfaces, text, accents, roles, stages, pillars, plus `--backdrop-tint` and `--shadow-fab`). Inventory built via `grep -rhoE 'var\(--[a-zA-Z0-9_-]+\)' srtd-next/src` plus the JS-string references for stage tokens (`'--c-stage-*'`). Every Plan-consumed token now has an explicit definition in both light and dark scopes — no fall-through to `:root` or its `prefers-color-scheme` media query. Plan is OS-theme-independent: `resolveTheme()` in `Plan.jsx:34` reads the system preference once at mount, writes `data-plan-theme`, and from there the Plan palette is sealed against the global cascade. **No JSX touched** — PR-C1 patches at Board.jsx:112/141/142/151, BriefsSection.jsx:164, AgeBadge.jsx, PeriodSheet.jsx, Plan.jsx Fab were already correct; the bug was purely in token cascade specificity. **No tailwind.css changes** — Plan wins locally via attribute selector specificity, not by mutating globals. Dark values mirror the existing dark set in `tailwind.css :root @media dark`; light values come from the warm-paper palette in tailwind.css `:root` plus PR-C1's vanilla-aligned stage colors (`--c-stage-brief:#6A5AA0`, `--c-stage-production:#3E7196`, `--c-stage-ready:#6B7E9A`, `--c-stage-input:#A85C3C`, `--c-stage-scheduled:#6E9068`, `--c-stage-approval:#C8841A`, `--c-stage-published:#147A4F`, `--c-stage-rejected:#D83838`, `--c-stage-parked:#8A857A`). Verified compiled `dist/style.css` contains all three blocks at offsets 23160, 24351, 25494 with `--c-bg-2: #F4F0E8` (light) / `#201E1A` (dark). 28 `?v=` strings bumped `20260425j → 20260425k`. React bundle rebuilt at `dist/sorted-react.js` 570.20 kB; `dist/style.css` 34.06 kB; `window.SortedReact.version` bumped `0.9.18 → 0.9.19`. No new deps, no Worker changes, no SQL changes. **Plan React token isolation invariant: every token Plan consumes MUST be declared in BOTH `[data-plan-root][data-plan-theme="light"]` AND `[data-plan-root][data-plan-theme="dark"]` (plus the matching `prefers-color-scheme: dark` block). Never let a Plan token fall through to `tailwind.css :root` — the global media query will override it whenever OS preference disagrees with the resolved Plan theme.**) Prior (2026-04-25) was (PR-3.7 PCS comment Read-more stray-dot fix. Single-file change at `srtd-next/src/flows/pcs/components/CommentRow.jsx:117-119`. The message body div replaced `${expanded ? '' : 'line-clamp-3'}` Tailwind className with an inline `style={expanded ? undefined : { maxHeight: 'calc(1.5em * 3)', overflow: 'hidden', textOverflow: 'clip' }}`. Root cause: `-webkit-line-clamp:3` paints an automatic WebKit ellipsis (`…`) at the truncation cutoff; in Fraunces serif at 18 px the three glyphs cluster tightly and read as a single stray dot above the READ MORE link on every truncated comment, light + dark. The new inline style mirrors the vanilla `.pcs-caption-text` pattern at `styles.css:10005-10020` ("no ellipsis — the See More button signals truncation") which has used `max-height` + `overflow:hidden` precisely to dodge this bug since launch. READ MORE button is unchanged — it still appears whenever `comment.message.length > 140`. No source data was touched; the dot was painted by the browser, not by JSX or text. Other comment cells (`ActivityFeed.jsx`, `Composer.jsx` reply preview, `FullScreenThread.jsx`) do not clamp comment body text, so the fix is single-site by construction. 28 `?v=` strings bumped `20260425i → 20260425j`. React bundle rebuilt at `dist/sorted-react.js` 570.20 kB; `window.SortedReact.version` bumped `0.9.17 → 0.9.18`. 552/552 unit tests still pass. No new deps, no Worker changes, no SQL changes.) Prior (2026-04-25) was (PR-C1 Plan React light + dark parity sweep. Fixes 5 components where Plan was hardcoded to the dark palette. **Owner initials badge** (`srtd-next/src/flows/plan/views/Board.jsx Card`): the 24px circle background was `#FFFFFF0D` (translucent white) — invisible against the cream `--c-bg` body in light mode. Inverted to filled-color: `background: ownerColor` (the role token, which already auto-flips light→dark via tailwind.css) + `color: #FFFFFF` text + font-weight 700 + font-size 8px. Now reads as a clearly-defined coloured pill in both modes. **AgeBadge overlay** (`srtd-next/src/flows/plan/shared/AgeBadge.jsx`): the `dark` prop forced `rgba(0,0,0,.55)` overlay always, which clashed with cream cards in light mode. Added `prefersDark()` matchMedia helper; `useDarkOverlay = dark && prefersDark()` gates the overlay. Light mode now falls through to the native tinted `bg` (color-mix at 18% alpha — bumped from 14%) + saturated text colour, integrating with the warm-paper palette. Dark mode keeps the legacy blur overlay unchanged. **Stage section headers** (`srtd-next/src/flows/plan/views/Board.jsx Row` + `BriefsSection.jsx Bucket` + `BriefsHeader`): the BRIEFS / AWAITING APPROVAL / SCHEDULED bands had no background — bold mono uppercase text on cream was reading as harsh black bands. Added `background: var(--c-bg-2)` + `borderBottom: 1px solid var(--c-divider-warm)` + softened text colour from `--c-text-mid` to `--c-text-soft`. In dark mode `--c-bg-2 = #201E1A` (subtle lift over body), in light mode `#F4F0E8` (subtle warm-grey divider band) — auto-flips via tailwind.css `:root` + `prefers-color-scheme`. **PeriodSheet backdrop** (`srtd-next/src/flows/plan/sheets/PeriodSheet.jsx`): hardcoded `rgba(0,0,0,.35)` aligned with the rest of the Plan sheets — now reads `var(--backdrop-tint, rgba(0,0,0,.3))` so it goes light-tinted (`rgba(20,18,15,.18)`) on warm-paper. **FAB shadow** (`srtd-next/src/flows/plan/Plan.jsx Fab` + `tokens.css`): swapped the inline 35%-black shadow for `var(--shadow-fab, ...)`. New `--shadow-fab` token in `tokens.css`: light = `0 10px 24px -10px rgba(120,80,60,.45)` (warm terracotta-tinted), dark = `0 12px 32px -8px rgba(0,0,0,.35)`. Verified Plan light + dark across Board, List, Calendar, Briefs, Insights — top bar / bottom nav / FilterBanner / Toast / DaySheet / FabSheet / Menu already used vars correctly and needed no change. **Out of scope**: dead `[data-plan-root] .plan-comment-card`/`plan-stats-*`/`plan-open-pcs-row`/`plan-carousel-*` rules in `tokens.css` (left from the killed MiniCard era) still carry hardcoded `rgba(255,255,255,...)` colours but are not consumed by any live JSX — left untouched per the "no refactors outside the listed patches" rule. 28 `?v=` strings bumped `20260425h → 20260425i`. React bundle rebuilt at `dist/sorted-react.js` 570.14 kB; `window.SortedReact.version` bumped `0.9.16 → 0.9.17`. **Plan React light + dark parity: every component must render correctly in both modes. Vanilla `tailwind.css :root` light + `@media (prefers-color-scheme: dark)` flip is the source of truth; never hardcode `#FFFFFF`/`rgba(0,0,0,...)` for surface colours. Verify both modes before any UI commit.**) Prior (2026-04-25) was (PR-3.6 Instagram-style PCS comment threading. Six coordinated patches across two files plus the shared Avatar primitive — replaces the inline parent quote bar with pure visual offset, drops the left rule, and aligns Composer rhythm with CommentRow. **PATCH 0 — Avatar primitive** (`srtd-next/src/core/ui/Avatar.jsx`): SIZE map updated so `sm` is the canonical 26-px small-avatar (was 22 px) and `md` is 36 px (was 24 px). `text-sm` lifted to `text-base` on `md` so a 36-px badge keeps its initial-letter legible. Three call sites: `ActivityFeed.jsx` (size=sm, reads new 26 px), `CommentRow.jsx` (now depth-aware per PATCH B), `PcsDetailSheet.jsx` OwnerSheet picker (size=md, picker rows grow 24→36 px which is within typical row-picker norms). lg=32 unchanged (no consumers). **PATCH A — reply offset** (`CommentRow.jsx:103`): the reply-branch className `ml-6 border-l-2 border-l-divider-warm pl-3` collapses to `ml-[70px] pl-0` — pure left indent at 70 px from the section edge, no spine, no card, no left rule. Tailwind v3 JIT supports the arbitrary value natively (config at `tailwind.config.js:18` already runs JIT). **PATCH B — depth-aware avatar** (`CommentRow.jsx:111`): `<Avatar size="md" />` becomes `<Avatar size={isReply ? 'sm' : 'md'} />`; `isReply = (comment.depth || 0) > 0` was already destructured at line 38 from PR-3.5. Replies now render at 26 px, parents at 36 px, so visual hierarchy comes from the avatar size + indent combination rather than the deleted quote bar. **PATCH C — quote bar deletion** (`CommentRow.jsx`): the `{parentDeleted && (...)}` and `{!parentDeleted && parent && (...)}` blocks (the "EARLIER IN THREAD" / parent-name / parent-message preview) are removed entirely. Visual offset from PATCH A communicates parent-child relationships now; the inline preview was redundant once replies were visibly indented. **PATCH D — dead-code sweep** after PATCH C: `resolveParent` import dropped from `CommentRow.jsx`; `byId` removed from the props destructure; `displayNameFromEmail` import dropped (was only used in the deleted `parentName` line). `CommentList.jsx` drops `byId` from the `filterAndIndex` destructure and from the `<CommentRow byId={byId} />` pass-through. `threading.js` drops the stale "CommentRow continues to call resolveParent for the parent quote bar" comment. The `resolveParent` export itself is retained per the PR-3.6 hard rule "no refactors outside the listed patches" — small isolated utility, no live callers, easy to remove in a follow-up if desired. **PATCH E — Composer rhythm** (`Composer.jsx:357`): the `<div className="flex items-end gap-2 px-3 py-2">` input row becomes `py-3` so the Composer's vertical padding matches the `py-3` already on every CommentRow container — top/bottom of the input aligns with the comment rows above it. Composer reply preview at line 296-303 (the "Replying to" header + truncate body + X button) is left untouched per spec — it's the Composer-side reply context, independent of the now-deleted CommentRow inline quote bar. **System dark/light auto-flip** is already wired via `tailwind.config.js:18 darkMode:'media'` plus the `prefers-color-scheme` swap in `src/styles/tailwind.css` — zero new work for PR-3.6. Manual toggle near profile is explicitly OUT OF SCOPE (deferred to PR-3.7). 28 `?v=` strings bumped `20260425f → 20260425h` (`g` reserved for PR-C1 #1042 per the PR conflict guard). React bundle rebuilt at `dist/sorted-react.js` 568.89 kB; `window.SortedReact.version` bumped `0.9.14 → 0.9.16` (`0.9.15` reserved for PR-C1). 552/552 unit tests still pass. No new deps, no Worker changes, no SQL changes, no channel/payload/RLS changes.) Prior (2026-04-25) was (PR-C1 Plan React agency data + role scoping. Five fixes layered on PR-B. **Fix 1 — agency stage order** (`srtd-next/src/flows/plan/shared/constants.js`): `STAGE_ORDER_BOARD_AGENCY = ['brief','awaiting_brand_input','in_production','ready','awaiting_approval','scheduled','published']` mirrors vanilla `01-config.js` PIPELINE_RENDER_ORDER plus `published` (Plan surfaces it as a Board row, vanilla pipeline does not). New `STAGE_ORDER_BOARD_CLIENT = ['awaiting_brand_input','awaiting_approval','scheduled','published']` is the safe subset of `CLIENT_ALLOWED_STAGES` minus `brief` (rendered via `BriefsAssignedSection`) and `in_production` (vanilla 07-post-load.js:380-385 only surfaces `in_production` rows to clients when comments exist on them — Plan does not surface it as a Board row). `STAGE_ORDER_BOARD` kept as an alias of `STAGE_ORDER_BOARD_AGENCY` for back-compat. `STAGE_LABELS` gains `brief: 'Brief'` and `ready: 'Ready'` so the new agency rows render with friendly labels. `Board.jsx` picks the right list at the export — `isClient ? STAGE_ORDER_BOARD_CLIENT : STAGE_ORDER_BOARD_AGENCY`. **Fix 2 — requests merge** (`srtd-next/src/flows/plan/store/planStore.js`): `applyPostsSnapshot` was stripping every `_isRequest:true` row from the bridge snapshot, which hid every brief from the agency Board. Vanilla `loadPosts` (agency) at `07-post-load.js:212-240` already converts pending+assigned `/requests` rows into brief-stage cards inside `AppState.posts.all` (stage forced to `'brief'`, owner from `r.assigned_to ? 'Creative' : 'Servicing'`, `_isRequest:true`), so the right answer was to KEEP those rows for non-client roles. Bridge now reads `get().role` and only strips `_isRequest:true` when role === 'client' — clients still render briefs through the dedicated `BriefsAssignedSection` / `BriefsCompletedSection` that read the separate `requests` slot via `loadRequests()`. Dedup is provided by vanilla `mergePosts`; the snapshot is already deduplicated by `getPostId()`. **Fix 3 — role-scoped filters** (`srtd-next/src/flows/plan/hooks/usePosts.js`): rewritten as `applyRoleScope(posts, role)` mirroring vanilla `render/pipeline.js` role gates. `client` keeps the existing `CLIENT_ALLOWED_STAGES` allowlist (PR-B). `creative` (mirrors pipeline.js:885-897): `brief` / `in_production` / `ready` gated to own-only via `(p.owner || '').toLowerCase() === 'creative'`; `awaiting_approval` + `awaiting_brand_input` show all rows; everything else hidden. `servicing` (mirrors pipeline.js:899-915): allowed stages `['brief','awaiting_approval','awaiting_brand_input','ready','scheduled']`, `brief` gated to own-only via `(p.owner || '').toLowerCase() === 'servicing'`. `admin` / `agency` unfiltered. Brief-assignment column is `posts.owner` (text, Title Case) — same column vanilla pipeline checks; no joins or new columns. **Fix 4 — role detection** (`srtd-next/src/flows/plan/store/planStore.js setRole`, `Plan.jsx resolveRole`, `initializeDefaultView`): `setRole` allowed-set extended to `['admin','agency','client','creative','servicing']` (was `['admin','agency','client']` collapsing creative+servicing to `'agency'`). `resolveRole` in Plan.jsx returns `'creative'` / `'servicing'` for matching effectiveRole instead of folding to `'agency'`. `initializeDefaultView` defaults map gains `creative: 'calendar'` + `servicing: 'calendar'` (same default as the agency umbrella). All existing `role === 'client'` and `role === 'admin'` consumers (`Calendar.jsx`, `Menu.jsx`, `Insights.jsx`, `FabSheet.jsx`, `DaySheet.jsx`, `Board.jsx`, `List.jsx`, `useLongPress.js`, `Plan.jsx`'s Fab gate) continue to work — creative/servicing simply pass `role !== 'client'` and `role !== 'admin'` checks, getting the agency code path for everything except the `usePosts` filter. **Fix 5 — owner color tokens + initials badge** (`srtd-next/src/styles/tailwind.css`, `Board.jsx`): `--c-role-creative` / `--c-role-client` realigned to vanilla `render/pipeline.js:361-365` — Creative `#9b87f5` (dark) / `#6B4FD9` (light, mirroring `--c-purple`); Client `#FF4B4B` (dark) / `#D83838` (light, mirroring `--c-red`); Servicing untouched (`--c-role-servicing` already `#22D3EE` dark / `#0891A0` light, matching vanilla `--c-cyan`). `Board.jsx Card` 20-px solid owner dot replaced with a 24-px circle showing `(post.owner || '').slice(0,2).toUpperCase()` initials, `#FFFFFF0D` bg, IBM Plex Mono 7-px, color = owner token — copies vanilla `pipeline.js:359-374` shape. `List.jsx` PostRow keeps its 3-px owner color bar (it is not a dot — prompt instruction "if it currently shows a dot" does not apply). **Drift from prompt notes**: vanilla agency `loadPosts` already merges `/requests` (status pending+assigned) into `AppState.posts.all` as brief-stage cards via `data.concat(reqData)` followed by `mergePosts` — no separate React-side merge code path was needed. The fix lived entirely in the bridge filter, not in `realtimeBridge.js`. The prompt suggested updating List.jsx to use initials too, but List currently shows a 3-px vertical color bar (not a dot), so it was left alone. `setRole` / `resolveRole` extension was required for the role split to actually distinguish creative from servicing — without it, both folded to `'agency'` and the new usePosts filters could not fire. 28 `?v=` strings bumped `20260425f → 20260425g`. React bundle rebuilt at `dist/sorted-react.js` 570.67 kB; `window.SortedReact.version` bumped `0.9.14 → 0.9.15`. No new deps, no Worker changes, no SQL changes, no channel/payload/RLS changes. **Plan React stage orders: `STAGE_ORDER_BOARD_AGENCY` includes `brief`, `awaiting_brand_input`, `ready`; client uses `STAGE_ORDER_BOARD_CLIENT` (separate). Requests merge into `planStore.posts` as brief-stage cards via vanilla `mergePosts` (agency path — bridge now retains `_isRequest:true` rows for non-client roles). Role filters in `usePosts`: client allowlist (`CLIENT_ALLOWED_STAGES`), creative own-only briefs, servicing scoped stages with own-only brief gate, admin/agency unfiltered.**) Prior (PR-B Plan React deep-links + brief open + client allowlist. Three correctness fixes layered on PR-A. **Fix 1 — deep-link unification** (`04-router.js`): when `?plan_react=1` is on alongside `?open=POST_ID`, the router queues the id into `window._planReactPendingOpen` and strips `?open=` via `history.replaceState` so vanilla's drain in `07-post-load.js renderAll` does NOT also fire (which would open vanilla `openPCS` underneath Plan's z-index 1400 layer). Flag-off path is unchanged — `window._pendingOpenPost` is still set so vanilla drains as before. **Fix 1b — drain in React** (`srtd-next/src/index.jsx`): after the existing `__pcsAutoOpenPostId` setTimeout(200), a sibling block reads `window._planReactPendingOpen` and branches on the `REQ-` prefix — briefs route to vanilla `window._openBriefSheet` (z-index 9500, mounts above Plan), regular posts route to `pcsFlow.open`. The 200 ms delay matches the existing pcs auto-open delay so activateRole + React mount have settled. **Fix 2 — brief tile open** (`srtd-next/src/flows/plan/views/BriefsSection.jsx`): the `BriefTile onClick` console.log stub now calls `window._openBriefSheet(request.id)` directly (`_openBriefSheet` already exposed at `render/brief.js:302`). **Fix 2b — pause/resume bridge** (`srtd-next/src/flows/plan/store/realtimeBridge.js` + `render/brief.js`): vanilla `_openBriefSheet` dispatches `sorted:brief-sheet-opened` immediately after `document.body.appendChild(overlay)` and a `MutationObserver` on `document.body` watches for the overlay's removal to dispatch `sorted:brief-sheet-closed` — single choke point covers every close path (BACK button, `_closeBrief`, `_reopenBrief`, `_createPostFromBrief`, `clientViewPost`) without refactoring 7+ inline `onclick` handlers. The bridge listens for both events and increments/decrements the existing PR-A pause counter, so brief-sheet activity is identical to any other Plan sheet from the realtime perspective. **Fix 3 — client allowlist** (`srtd-next/src/flows/plan/hooks/usePosts.js`): rewritten from `CLIENT_STAGE_DENYLIST = ['in_production', 'ready']` to `CLIENT_ALLOWED_STAGES = ['awaiting_approval','awaiting_brand_input','published','brief','scheduled','in_production']`, copied verbatim from the vanilla server-side filter at `07-post-load.js:307-308`. Defense in depth — vanilla's allowlist is the only thing keeping internal stages off the client wire, but if a future merge regression sends an extra row to the React client, the strict allowlist still drops it. **Fix 4 — Board client stages** (`srtd-next/src/flows/plan/views/Board.jsx:174`): `rejected` and `parked` removed from the client visibleStages array. Resulting list `['awaiting_brand_input','awaiting_approval','scheduled','published']` is the safe subset of `CLIENT_ALLOWED_STAGES` minus `brief` (rendered via `BriefsAssignedSection` above) and `in_production` (vanilla 07-post-load.js:380-385 only surfaces `in_production` rows to clients when comments exist on them — Plan does not surface `in_production` as a Board row). **Drift from prompt notes**: vanilla brief sheet z-index is already 9500 (above Plan's 1400), so no CSS change was needed. `_openBriefSheet` is already on `window`, so no new export was needed. `srtd-next/src/index.jsx` already had a `__pcsAutoOpenPostId` drain at line 38-43, so the new drain mirrors that pattern instead of creating a separate one. The vanilla brief sheet has 7+ inline `onclick="document.getElementById('brief-sheet-overlay').remove();"` close paths plus 4+ JS-side `overlay.remove()` calls; emitting `sorted:brief-sheet-closed` from each would have meant editing 11 sites, so a single `MutationObserver` on `document.body` watches for the overlay's removal and dispatches once. 28 `?v=` strings bumped `20260425e → 20260425f`. React bundle rebuilt at `dist/sorted-react.js` 569.78 kB; `window.SortedReact.version` bumped `0.9.13 → 0.9.14`. No new deps, no Worker changes, no SQL changes, no channel/payload/RLS changes. **Plan React deep-links: `?open=POST_ID` with `plan_react` flag routes briefs to vanilla `_openBriefSheet` (above Plan z-index) and posts to `SortedReact.pcsFlow.open`. Vanilla brief sheet emits `sorted:brief-sheet-opened`/`closed` events to pause/resume `realtimeBridge`.** **Client allowlist (`usePosts.js`) mirrors vanilla server-side stage filter — any change to vanilla `07-post-load.js` client filter must be mirrored here.**) Prior (2026-04-25) was (PR-A Plan React realtime spine. Bridges vanilla's existing Supabase realtime stream into the React Plan tree via two window events — vanilla still owns every channel + the liveness watchdog; React only consumes snapshots. **Vanilla event emitters**: `mergePosts` in `07-post-load.js` now dispatches `window.dispatchEvent(new CustomEvent('sorted:posts-updated', { detail: { posts: AppState.posts.all, requests: AppState.posts.requests || [] } }))` after `setAll`/cache update — covers every code path (initial load, agency Realtime refresh, drainPollStash, client refresh, mark-task-done sync). `_recomputeBadgeLocal` in `10-ui.js` dispatches `sorted:notifications-updated` with `{ notifications: window._notifData, unreadCount }` as the single choke point: every mutation of `_notifData` (loadNotifications, mark-read, delete, mark-all, realtime INSERT) ends in `_recomputeBadgeLocal` so a single emit per mutation is sufficient. **New file** `srtd-next/src/flows/plan/store/realtimeBridge.js`: subscribes to both events, pushes snapshots into planStore via new `applyPostsSnapshot`/`applyNotificationsSnapshot` actions. Tracks a refcounted pause counter — while > 0, snapshots are stored in pending slots (overwrite, never queue) and applied on `resumeRealtime` when counter hits 0. Idempotent: starting twice returns the same `stopFn`. Never imports `@supabase/supabase-js` or `apiFetch` — pure window-event consumer. **planStore.js** gains two state slots (`notifications: []`, `unreadCount: 0`) plus the two snapshot actions. Posts snapshot strips `_isRequest:true` rows because vanilla folds /requests into posts.all but Plan tracks them separately via `loadRequests()`. Empty `requests` payload in detail leaves the existing requests slot intact. **Plan.jsx** now imports from `realtimeBridge.js` + `usePcsFlowState`. New `useEffect` mounts the bridge on mount (gated on `planEnabled`) with a stopFn cleanup. Two pause/resume `useEffect`s — one watches `activeSheet` (any non-null sheet pauses), one watches `pcsOpen` from `usePcsFlowState` — useEffect cleanup gives try/finally semantics so a thrown render inside a sheet still resumes. Bell stub at the topbar replaced with a real 8-px terracotta dot anchored top-right when `unreadCount > 0`; the click handler still toasts "Notifications panel coming in PR 2" — full panel is PR-B. Plan flag stays `?plan_react=1`. 28 `?v=` strings bumped `20260425d → 20260425e`. React bundle rebuilt at `dist/sorted-react.js` 569.02 kB. **Drift from prompt notes**: planStore.js lives at `srtd-next/src/flows/plan/store/planStore.js`, not `state/planStore.js`. `AppState.notifications` does not exist as a fresh array — vanilla notifications live in `window._notifData` (top-level `let` in 10-ui.js); the dispatch surfaces that array via the event detail. `AppState.posts.requests` also does not exist — vanilla folds requests into `posts.all` with `_isRequest:true`; the bridge filters them out at the React boundary. No new deps, no Worker changes, no SQL changes, no channel/payload/RLS changes.) Prior (2026-04-25) was (PR-3.6 PCS visual threading + relative time + latest-thread-first sort. Three coordinated changes on top of PR-3.5. **Change 1** new util `srtd-next/src/flows/pcs/utils/timeAgo.js` — pure function, no React: returns `'just now'` (<30 s), `'<n>s ago'` (<60 s), `'<n>m ago'` (<60 min), `'<n>h ago'` (<24 h), `'<n>d ago'` (<7 d), `'<n>w ago'` (<4 w), `'<n>mo ago'` (<12 mo), then `'<n>y ago'`. Defends against null/undefined/NaN by returning `''`. **Change 2** `srtd-next/src/flows/pcs/utils/threading.js` `filterAndIndex` — flat newest-first sort replaced with thread-aware sort. Algorithm: build `byId` of ALL comments → for each comment walk `reply_to` chain via `byId` to find its root (cycle-safe via `seen` Set, missing-parent fall-through treats the orphan as its own root) → group comments by root id → compute `latestActivity` per thread as `max(created_at)` across parent + all replies → sort threads by `latestActivity` DESC → within a thread emit root first then replies sorted by `created_at` ASC → return flat array of comment copies each carrying a `depth` field (0 for root, 1+ for replies). `byId` still holds the original objects so `resolveParent` consumers in `CommentRow` are unchanged; `resolveParent` signature is preserved exactly. **Change 3** `srtd-next/src/flows/pcs/components/CommentRow.jsx` — two changes: (a) replaced `formatCommentTime` import with `timeAgo` from `../utils/timeAgo.js` and the `time` constant now reads `timeAgo(comment.created_at)` so every comment shows relative time instead of `MMM D · h:MM AM/PM`; (b) added `isReply = (comment.depth || 0) > 0` and templated the row container className so depth>0 rows get `ml-6 border-l-2 border-l-divider-warm pl-3` — a left rule keyed off the existing `divider-warm` token gives a single-step visual indent regardless of nesting depth. Quote-bar logic at lines 121-132 untouched: indent + quote bar work together (indent says "this is a child", quote bar says "of this parent"). 28 `?v=` strings bumped `20260425c → 20260425d`. React bundle rebuilt at `dist/sorted-react.js` 566.96 kB. 552/552 unit tests pass. No new deps, no Worker changes, no SQL changes.) Prior (2026-04-25) was (PR-3.5 PCS threading reply wiring — two-line fix for the threading bug deferred from PR-3. **Bug 3** root cause was render-side, not data-side: `post_comments.reply_to` is a uuid-nullable FK and 91/392 rows had it populated, but every reply triggered from the "View all comments" overflow view shipped with `reply_to:null`. **Fix 1** `srtd-next/src/flows/pcs/components/FullScreenThread.jsx`: the `onReply` prop passed to `CommentList` was a no-op stub `() => {}`, so tapping Reply inside the full-screen overflow never set `replyTo` on the parent Composer. Added `onReplyToComment` to the component's prop signature and replaced the stub with `(c) => { onReplyToComment(c); onClose(); }` — lifts the reply intent up to PCS.jsx and closes the overlay so the Composer becomes visible with the quote preview, exactly mirroring the inline `CommentList` reply path. **Fix 2** `srtd-next/src/flows/pcs/PCS.jsx`: tab-switch leak — `replyTo` set on the comments tab leaked into an `internal_notes` POST if the user switched tabs without cancelling. Added `useEffect(() => { setReplyTo(null); }, [activeTab])` after the existing `setReplyTo` state declaration; `useEffect` added to the React import. Both `<FullScreenThread>` instances (comments + internal notes) now receive `onReplyToComment={onReplyToComment}` so the lifted handler reaches the overflow view in either tab. PR-3.5 deliberately scoped to threading-reply wiring only — visual indentation of nested replies (CommentRow `ml-*`) was identified as a design decision (threading is currently rendered via a colored quote bar, not DOM nesting) and is not a bug. 28 `?v=` strings bumped `20260425b → 20260425c`. React bundle rebuilt at `dist/sorted-react.js` 565.96 kB. No new deps, no Worker changes, no SQL changes.) Prior (2026-04-25 earlier) was (PR-3 PCS comments comprehensive fix — five bugs across nine files. **Bug 1** `srtd-next/src/flows/pcs/components/Composer.jsx`: `currentRole` now reads `s.user?.effectiveRole || s.user?.role` so admin-preview sessions never fall back to the unset DB role. The ad-hoc `charAt(0).toUpperCase() + slice(1).toLowerCase()` title-case is replaced with an explicit map (`admin → Admin`, `servicing → Servicing`, `creative → Creative`, `client → Client`) and an early guard that toasts `Session role missing, please refresh` + logs `composer_missing_role` when the role is unrecognised — protects `post_comments.author_role` from ever receiving an empty string or `Undefined`. **Bug 2** `srtd-next/src/flows/pcs/pcsStore.js` + `components/CommentRow.jsx`: Read-more expansion was lost on every realtime echo because `useState(false)` reset on remount. Hoisted to the store as `expandedComments: Set<id>` + `toggleExpanded(id)` action; reset() clears it. CommentRow drops local state, reads from the store, threshold tightened `180 → 140` to match `line-clamp-3` visual clip. **Bug 4** `srtd-next/src/flows/pcs/index.js`: avatars rendered as initial badges on cold-open because `listUserRoles()` was awaited sequentially after `fetchCommentsAndReactions()` — the realtime channel fired its first echo while userRoles was still resolving. Both `pcsFlow.open()` and `refreshRealtime()` now run a single `Promise.all([listUserRoles(), fetchCommentsAndReactions(postId)])` so the roster is always present when comment rows mount. Single try/catch with telemetry context `pcs_react_open` (was two separate blocks). **Bug 5** vanilla mention writers — `notify-comment` edge silently drops any mention whose `author_role` cannot be resolved, so the four vanilla insert sites that pushed mention NAMES into `mentioned_users` produced zero notifications. Fixed in lockstep: `render/client.js _handleSubmitComment` (now async; new helper `_resolveMentionNamesToEmails` reads the cached `_clientMentionRoster` with a `_fetchClientMentionRoster()` fallback, drops unresolved); `render/brief.js` (new helper `_resolveBriefMentionsToEmails` reads `_briefTeamMembersCache || _pcsRosterData`, wired into the payload at line 234-242); `10-ui.js _notifSubmitReply` (replaces `mentioned.push(_rn)` with `mentioned.push(m.email)`, drops the regex fallback since it has no roster context); `actions/pcs.js submitPcsComment` (awaits the existing `_lookupMentionEmails` helper right after `_mentioned` is built, maps to emails, drops unresolved on lookup error). **Bug 6.1** `Composer.jsx`: composer had no optimistic insert — clients saw their comment vanish for 400-1200 ms before the realtime echo refreshed the list. Now inserts a `_temp: true` row keyed by `tempId = temp-<ts>-<rand>` into `comments` immediately, then awaits createComment in a try/catch. On catch, removes the temp row and restores `setText(originalText)` so the user can retry. After `retryComments()` resolves, dedupes the temp row only when a real row landed with matching author + message and `created_at` within 60 s. Internal-notes path stays await-only because Bug 6.1 was scoped to client-visible comments. **Bug 6.2** `srtd-next/src/flows/pcs/PCS.jsx`: stage transitions (e.g. brand_input → awaiting_approval) did not refresh visibility-filtered comments. `pcsFlow.retryComments()` now fires immediately after `updatePostStage()` resolves in `onClientApprove`. Fire-and-forget — does not block the optimistic UI. **PR-3.5 deferred**: Bug 3 (threading.js — `reply_to` populated in DB but tree not rendering) needs a targeted re-audit before patching; Branch C identified without file:line during the initial pass. 28 `?v=` strings bumped `20260425a → 20260425b`. React bundle rebuilt at `dist/sorted-react.js` 565.86 kB. 552/552 unit tests pass. No new deps, no Worker changes, no SQL changes.) Prior (2026-04-24) was (Kill MiniCard in srtd-next Plan. Every tile tap in Plan now opens full PCS — no more intermediate bottom sheet. Deleted `srtd-next/src/flows/plan/sheets/MiniCardSheet.jsx` (1106 lines), `KebabMenu.jsx` (178 lines), `RecallSheet.jsx` (118 lines). NudgeSheet never existed as a file — its flow was inline in MiniCardSheet.onKebabAction and has been ported verbatim into the new `srtd-next/src/flows/pcs/components/PcsKebabMenu.jsx`. **New shared helper** `srtd-next/src/flows/plan/shared/openInPcs.js` — single entry point `openInPcs(post, contextPosts)` that reads `window.SortedReact.flows.pcs.open(postId, { contextList })` with the current filtered post_id list. Fallback: URL deep-link `?pcs_post=<id>` when bridge missing. **Rewired tile taps** in `Board.jsx` (Card onClick + Row/Board contextPosts plumbing), `Calendar.jsx` (ThumbMini onClick — preserves `window.__planDragActive` drag guard; Cell/MonthBlock/WeekDayRow/WeekBlock contextPosts plumbing; month view uses full flat `posts`, week view uses `weekPosts`), `List.jsx` (rowClickGuarded preserves 400ms long-press guard; PostRow contextPosts from List export), and `DaySheet.jsx` (handleOpen snapshots day's `posts`, closes day, setTimeout 240ms → openInPcs — preserves sequential transition animation). **PCS prev/next navigation**: `pcsStore.js` gains `contextList: []` slot + `setContextList(list)` action (reset() clears it). `pcsFlow.open(postId, opts)` now accepts `{ contextList }` and persists to store. New `pcsFlow.navigate(direction)` resolves current postId → finds idx in contextList → calls `pcsFlow.open(nextId, { contextList: list })` preserving context. No-op at list edges or when list empty. Logs `pcs_react_navigate` telemetry. `KickerRow.jsx` gains ChevronLeft/ChevronRight buttons to the left of the existing close X, rendered only when `contextList.length > 1 && idx >= 0` — single-arg open callers (notification deep-link, auto-open, vanilla-card-click delegate) leave the list empty so chevrons stay hidden. **Agency kebab parity**: new `PcsKebabMenu.jsx` mounts in KickerRow to the left of the PanelRight detail-sheet button, hidden for client role. Stage matrix: `brief`→assign+rework_brief, `in_production`/`ready`→recall+send_for_approval, `awaiting_approval`/`awaiting_brand_input`→recall+nudge_client, `scheduled`→recall+reschedule+publish_now, `rejected`/`parked`→revive, `published`→[]. **Recall** uses an inline "Recall to" submenu in the same popover (no separate RecallSheet bottom sheet); picker lists earlier canonical stages via RECALL_STAGE_ORDER (brief→published). Writes `activity_log.action='recalled'` alongside the normal audit_log entry. **Stage writes** go through existing `useOptimisticPatch().commit()` with `{ stage: true, auditField: 'stage', actor }` — same optimistic UI, 5s undo toast, RETRY toast as PcsDetailSheet. **nudge_client** ports the exact MiniCardSheet flow: `window.prompt` → null-safe early return → createComment with visibility:'all' + post_title + session-expired guard + Title-Case effective role. `reschedule`/`assign`/`rework_brief` delegate to `onOpenSheet` (PcsDetailSheet already has date picker + owner list). `publish_now` confirm() → saveStage('published'). Dismiss on outside-click / touchstart / Escape. All icons lucide-react. **planStore.js cleanup** removed `currentPost` field, `openMiniCard`/`closeMiniCard`/`navigateMiniCard`/`loadCaptionFor` actions, `fetchCaptionAndImages` import, and all `currentPost` references in `rescheduleTarget` (4 sites including rollback). `closeSheet` no longer clears currentPost slot. **Plan.jsx** drops MiniCardSheet import + mount gate. **planApi.js** removes `fetchCommentsForMiniCard`, `fetchActivityForMiniCard`, `fetchCaptionAndImages`, `patchStage` — all were MiniCard-only after the grep audit. **List.jsx knock-on**: commitTitle no longer syncs currentPost, just updates flat posts array. **CSS keyframe** `planMiniCardSheetSlideUp` / class `.plan-mini-card-sheet-enter` preserved — still consumed by DaySheet/Menu/FabSheet as shared bottom-sheet slide-up. Client-role PCS still hidden kebab (PR 1 read-only contract intact). 28 `?v=` strings bumped `20260424j → 20260424k`. React bundle `window.SortedReact.version` `0.9.12 → 0.9.13`, dist/sorted-react.js 563.76 kB. No new deps, no Worker changes, no SQL changes.) Prior (2026-04-24) was (Kill the `brief_done` stage end-to-end. DB migration already ran; this PR scrubs every code reference. Stripped from `01-config.js` (STAGE_META entry + STAGE_ORDER), `06-post-create.js` (legacy brief-close path now writes `stage: 'parked'`), `07-post-load.js` (allowedStages list, deep-link brief routing, client card-click delegate), `08-post-actions.js` (_stageLabel map), `10-ui.js` (_NOTIF_MOVES_TYPES + _NOTIF_STAGE_LABELS + isBriefAttr), `render/brief.js` (_isBriefDone now constant-false; _closeBrief legacy path writes `stage: 'parked'`; request close no longer touches AppState stage), `render/pipeline.js` (pipelineStages + stageDisplayMap + stageColorMap + _clientStages + whole Closed Briefs group deleted from _renderPipelineInner + container.innerHTML assembly), `styles.css` (client-mode chip hide selector), and every `srtd-next/` consumer (`core/mappings.js`, `flows/pcs/utils/stage.js`, `flows/pcs/components/PcsDetailSheet.jsx` stage picker, `flows/plan/shared/constants.js` STAGE_LABELS + STAGE_COLOR_VAR + STAGE_ORDER_BOARD, `flows/plan/sheets/KebabMenu.jsx` STAGE_MATRIX, `flows/plan/sheets/RecallSheet.jsx` STAGE_ORDER, `flows/plan/sheets/DaySheet.jsx` AGED_STAGES, `flows/plan/sheets/MiniCardSheet.jsx` AGED_STAGES, `flows/plan/views/List.jsx` AGED_STAGES). **Client plan Board reflow**: `flows/plan/views/Board.jsx` now renders for client role as BRIEFS (assigned requests) → post-stage rows (awaiting_brand_input, awaiting_approval, scheduled, published, rejected, parked) → BRIEFS COMPLETED (closed requests). `flows/plan/views/BriefsSection.jsx` gains two new exported components `BriefsAssignedSection` and `BriefsCompletedSection` that wrap the existing `Bucket` primitive; the original `BriefsSection` stays exported for any other caller. Agency board layout unchanged. `usePosts.js` CLIENT_STAGE_DENYLIST already correct at `['in_production', 'ready']`. Tests updated: `tests/deeplink.test.js` drops the brief_done stage assertion, `tests/notif-render.test.js` removes brief_done from the _NOTIF_MOVES_TYPES + _NOTIF_STAGE_LABELS sandbox and the "moves" chip assertion, `tests/notifications.test.js` drops the "'brief_done' -> 'Closed Brief'" assertion, `tests/e2e/client-feed.spec.js` removes brief_done from internalStages. React bundle rebuilt. 28 `?v=` strings bumped `20260424d -> 20260424e` — `c` was skipped because a prior PR already shipped `d`, so using `c` would have rolled Cloudflare back to a stale build.) Prior (2026-04-22) was (Canonicalise `post_comments` writes end-to-end so `author` is always the session email, `author_role` is always Title-Case, and `attachments` always hits the table as the JSON-stringified shape that `normalizeAttachments()` expects. All four vanilla insert sites rewritten: `render/brief.js _briefSubmitComment` (now reads `authorEmail` from `AppState.user.email` and writes that instead of the name), `render/client.js _handleSubmitComment` (writes `_emailKey` as `author`, Client role unchanged), `10-ui.js _notifSubmitReply` (notification-drawer reply path — writes `_emailKey` as `author`), and `actions/pcs.js submitPcsComment` + `_doSubmitComment` (in-PCS agency composer — `_author` is now `_emailKey`, not the resolved display name). Every one of those sites short-circuits before `apiFetch()` with `showToast('Session expired, please refresh', 'error')` when `AppState.user.email` is empty, so a stale/logged-out session never writes an attributable-to-nobody row. `_doSubmitComment` callers updated accordingly: `submitPcsComment` at `actions/pcs.js:2833` and `:2847`, plus the `_pendingComment` confirm flow at `index.html:1098` which is assembled from `actions/pcs.js:2815` — all three now carry `_author = _emailKey` through the dispatcher. On the React side, `srtd-next/src/flows/pcs/components/Composer.jsx onSend` does THREE things: (a) Title-cases `currentRole` via `role.charAt(0).toUpperCase() + role.slice(1).toLowerCase()` before sending as `author_role`, because PostgREST `eq.` on `post_comments.author_role` is case-sensitive and the agency feed filters use `eq.Admin` / `eq.Creative` / `eq.Servicing`; (b) `JSON.stringify`'s the attachment payload before POST — single-part goes as `JSON.stringify(attachmentParts[0])`, multi-part as `JSON.stringify(attachmentParts)` — matching the invariant that `normalizeAttachments()` in `srtd-next/src/flows/pcs/utils/attachments.js` parses 1-to-2 times before unwrapping (some Supabase client paths historically double-encoded, and the normalizer has to defensively re-parse); (c) hard-guards `!currentEmail` with the same session-expired toast and early return. New top-of-file invariant line ("`post_comments` writes: author must be `AppState.user.email`, author_role must be Title-Case, attachments must follow `normalizeAttachments` invariant — see `Composer.jsx` onSend") added so every future comment-writing PR has the contract at eye level. 28 `?v=` strings bumped `20260421k → 20260422a`. React bundle rebuilt at `window.SortedReact.version === '0.9.11'`. 553/553 unit tests pass.) Prior (2026-04-21 later) was (React PCS caption save — three coordinated fixes. **FIX 1 — logError bridge** (`srtd-next/src/core/bridges/logging.js`): accept EITHER `context.action` OR `context.context` as the `error_log.action` label, so existing React PCS call sites using `{ context: 'pcs_react_*' }` surface the real action instead of defaulting to `'react-unknown'`. Every React-side telemetry row now carries a meaningful action, making error-log triage tractable. **FIX 2 — Caption save retry** (`srtd-next/src/flows/pcs/components/CaptionBlock.jsx`): wrap `patchPost` in a one-shot retry that waits 800 ms and re-attempts ONLY when the thrown error matches `/^Load failed$/i`, `/^NetworkError/i`, or `/^Failed to fetch/i` (the three Safari/iOS network-blip shapes). Any other error rethrows to the existing catch. Toast branches to `'Network blip, please try again'` on those network patterns, otherwise keeps `'Save failed'`. `writeAudit` stays fire-and-forget after a successful PATCH. The PATCH body is idempotent so retry is safe. **FIX 3 — audit_log columns** (`srtd-next/src/core/api/audit.js`): `writeAudit` POST body now sends `{ action, changed_by, changed_at }` — the live Mumbai Supabase schema names, not the legacy `{ field, actor, created_at }`. `listAuditForPost` selects `id,post_id,action,old_value,new_value,changed_by,changed_at` and orders by `changed_at.desc`. Because `utils/activity.jsx` and `components/ActivityFeed.jsx` still read `row.field` / `row.actor` / `row.created_at`, `listAuditForPost` maps each returned row back to those keys at the network boundary — consumer shape is unchanged, only the wire protocol moved. 28 `?v=` strings bumped `20260421j → 20260421k`. React bundle `window.SortedReact.version` bumped to `0.9.11`.) Prior (2026-04-21 earlier) was (Client comment textarea persistence — fixed the bug where typing a comment in the client feed was wiped every time a realtime refetch fired (400 ms after any post/comment/request/notification change elsewhere). Root cause: `render/client.js` was baking the textarea into the full-feed HTML and replacing everything with `cv.innerHTML = html` on each refresh, destroying the in-progress textarea. The iOS-Safari activeElement guard at `07-post-load.js:667` didn't help because scrolling dismisses the keyboard and moves focus off the input. Fix: HOIST the composer out of the innerHTML rebuild path. `_commentsContainerHtml` now emits only `<div data-comments-input-section="<pid>" data-needs-composer="true"></div>` as an empty slot for the composer. A new `window._persistentComposers` cache keyed by `post_id` holds the live composer element (textarea, pending-image state, quote bar, wired listeners). Before `cv.innerHTML = html` runs, `_detachAllPersistentComposersIn(cv)` pulls live composers into the cache; after the rebuild, `_mountPersistentComposerIntoSlot` moves the cached element into the freshly-rendered slot via `appendChild` (preserves DOM state — NOT cloned). The SAME element also moves between the feed card and `_openClientPostOverlay` via a new `_handOffComposerToFeed` helper in both overlay close paths (X button + Back-to-notifications button), so typed text survives card->overlay->card transitions too. Wiring is one-shot per composer element via a `_srtdWired` flag; `_wireCommentInputFocus` now checks the flag and returns early, so multiple `renderClientView()` calls don't stack focus/input listeners. Comment list still rebuilds via innerHTML inside a new `<div data-comments-list-section="<pid>">` wrapper — only the composer is hoisted. No changes to `07-post-load.js` realtime debouncing, `_handleSubmitComment` submit/optimistic/restore logic, React PCS, vanilla PCS, or brief sheet. 28 `?v=` strings bumped `20260421i -> 20260421j`. 553/553 unit tests still pass.) PR 2.2 (prior) was (full-screen PCS field editors via new `FullScreenEditor` primitive; `PropertySheet` / `OwnerSheet` / `StageSheet` all rebuilt on top of it so no field editor slides up as a bottom sheet any more. `CommentActionSheet` stays a bottom sheet because it's a contextual menu, not a field editor. Title inline-edit: "Edit title" pencil row in `PCS.jsx` deleted; `Topbar` title span is now a `<button>` that calls `onEditTitle` when `canEdit`. Native iOS date picker fixed — `PropertySheet` date branch skips programmatic focus, uses tap-only on a 56-px input with `WebkitAppearance:none`, auto-saves on change. Typography contrast bump: `--c-text-dim` in `src/styles/tailwind.css` lifted `#A8A498 -> #8A857A` (light) and `#4A4640 -> #6A655C` (dark). `Avatar` gains `loading="eager" decoding="async"` so slow R2 profile pictures don't flash the initial-badge fallback. 28 `?v=` strings bumped `20260418y -> 20260418z`. React bundle `window.SortedReact.version` `0.9.0 -> 0.9.1`.) B5.5a.5 (prior) was ( — full rebuild of the React Caption Workspace at `srtd-next/src/shared/caption-workspace/` as a Tailwind-native component tree with Lucide icons. Deleted the four-file MVP shell (`CaptionWorkspace.jsx` / `store.js` / `api.js` / `systemPrompts.js`) and rebuilt as 11 files: `utils.js` (cost math, caption extraction, memory regex, option splitter, JSON-loose parser), `systemPrompts.js` (angles + write + refine + review + rewrite + title-synth builders), `api.js` (feature-remapped Worker calls — angles/write/refine/rewrite → 'writer'; review → 'qc'; **no Worker change**; memory/cost helpers), `store.js` (Zustand — full flow: brief+title, headerExpanded, messages thread, isSending, session/today/month cost, memory+corrections, onUse/onClose callbacks; actions open/close/toggleHeader/updateBrief/requestAngles/requestDraftFromAngle/applyRefineChip/updateDraftContent/finaliseDraft/runReview/toggleReviewFlag/regenerateFromFlags/sendUserMessage/useThis with memorize: shortcut), `CaptionWorkspace.jsx` (top-level slide-up overlay shell, z-index 9600, body-scroll lock, Esc closes, max-width 480 centered), `components/Header.jsx` (coupled accordion — Row 1: X · Sparkles+title · cost meter; Row 2 expanded: contenteditable brief + attachment chip + Edit/Regen-angles), `components/Thread.jsx` (auto-scroll, renders user bubbles / memorized-pulse rows / AnglesCard / DraftCard / ReviewBlock + "Thinking" typing dots), `components/AnglesCard.jsx` (3 tappable rows → requestDraftFromAngle), `components/DraftCard.jsx` (contenteditable body, Shorter/Warmer/Sharper/Review chips, primary swap — default Finalise → terracotta "Use this" when finalised → green "Use this" when review all-PASS; finalised draft gets amber shadow-memo ring), `components/ReviewBlock.jsx` (checkbox FLAG rows opt-out, locked green PASS rows, "Regenerate · fix N" disabled when 0), `components/Composer.jsx` (auto-grow textarea + ArrowUp send; Enter sends / Shift+Enter newline), `components/icons.js` (Lucide re-exports). Flow: 3 angles → draft → Finalise → Review (checkbox flags, default all checked) → Rewrite addressing selected flags → repeat until all-PASS → green Use this. No dimming of past content when regenerating. Full memory parity (loadMemory / saveUserInstruction / saveCorrection). Bridge interface unchanged: `openCaptionWorkspace(mode, opts)` still routes to `useCaptionWorkspaceStore.getState().open()`. Vanilla `actions/pcs-claude-caption.js` and `#caption-workspace-overlay` remain 100% untouched — PCS Polish / QC / Rewrite keep consuming the DOM-backed overlay. Worker **NOT TOUCHED** — review mapped to 'qc' feature, all other actions to 'writer'; custom prompts ride the user-message content so the B5.5a.1 defensive role filter stays satisfied. All 28 `?v=` strings bumped `20260418o → 20260418p`. React bundle `window.SortedReact.version` bumped `0.8.1 → 0.9.0`. 553/553 unit tests still pass. No new deps, no Worker changes, no SQL changes, Import Brief sheets (Paste / Gmail / Upload-stub) deliberately untouched — unified inline Import is deferred to a follow-up PR. B5.5a.4 — renamed `pcs-next/` → `srtd-next/` via `git mv` (history preserved) and cloned the Caption Workspace as a pure-React component. The vanilla overlay's DOM nesting bug — `#caption-workspace-overlay` lives inside `#pcs-overlay`, which is `display:none` for every React-only flow — bit the Create Post ⤢ path, so instead of patching vanilla we forked it into React. New folder `srtd-next/src/shared/caption-workspace/` with four files: `CaptionWorkspace.jsx` (full-screen slide-up overlay, z-index 9600, warm-ink palette, max-width 480 centered), `store.js` (Zustand — `isOpen`, `mode`, `messages`, `sessionCost`, `onUse`, `onClose`, `open/close/sendUserMessage/applyDraft`), `api.js` (thin wrapper around srtd-ai Worker `/ai/complete` with INR cost math), `systemPrompts.js` (write-mode prompt builder — brief-first with title/pillar fallback). v1 MVP scope: write mode (3 numbered options, split into separate option cards with per-card "Use this" button) + chat mode (iterative thread) + live session-cost chip — explicitly no Polish, no QC, no Rewrite, no ai_memory read/write. Bridge `srtd-next/src/core/bridges/captionWorkspace.js` now calls `useCaptionWorkspaceStore.getState().open(mode, opts)` instead of proxying to `window.openCaptionWorkspace` — so `CaptionField.handleOpenWorkspace` (⤢ from Create Post) and `PasteSheet` admin path open the React workspace directly; vanilla `window.openCaptionWorkspace` is left 100% untouched so PCS Polish / QC / Rewrite keep working unchanged. `App.jsx` mounts `<CaptionWorkspace />` unconditionally alongside `<CreatePost />` so the overlay floats above whatever flow is active. `index.html` script src `/pcs-next/dist/sorted-react.js` → `/srtd-next/dist/sorted-react.js`. All 27 `?v=` strings bumped `20260418m → 20260418n`. React bundle `window.SortedReact.version` bumped `0.7.2 → 0.8.0`. 553/553 unit tests still pass. `getSessionCost()` in the bridge now reads from the React store, so Create Post's 600 ms cost-chip poll updates live from React; `CaptionField.onClose` falls back to `window._captionWS.sessionCost` only if the new bridge isn't available. No new deps, no Worker changes, no SQL changes.) Full history: `CLAUDE-archive-20260411.md`.

## 1 — WHAT IS SORTED

Social media content ops platform for agencies — workflow + client trust, NOT a scheduler.

Stack: Vanilla JS (no framework, no build step), Supabase (PostgREST + auth + Realtime), Cloudflare Workers + R2, GitHub Pages, Resend (email). Client-side Realtime uses `@supabase/supabase-js@2.45.4` loaded as a UMD bundle from jsDelivr (see `index.html`); all REST traffic still flows through `apiFetch()`, the SDK is ONLY used for the Realtime WebSocket.

Repo: `github.com/Guneygaar/guneygaar.github.io`. ALL files at REPO ROOT. Never reference `/sorted/` — it does not exist. Subdirs: `render/ actions/ tests/ tests/e2e/ sorted-preview-worker/ sql/ preview/ mockups/`.

Deploy: branch `main-/-root` → `srtd.io` (Cloudflare Pages).

Team: Shubham (Admin), Chitra (Servicing), Pranav (Creative), Manisha + Shivangini (Client).

## 2 — DB SCHEMA

Supabase: `ozptjplxbyswclolbxyn.supabase.co`. Always `apiFetch()`, never raw `fetch()`. PostgREST `eq.` is CASE-SENSITIVE — use Title Case roles (`Admin`, `Servicing`, `Creative`, `Client`).

**posts** — PK `post_id` text
| col | type | notes |
|---|---|---|
| id | uuid | |
| post_id | text | PK |
| title, stage, owner, content_pillar, location, format, caption, canva_link, linkedin_link, internal_notes, client_feedback, linked_post_id, created_by, updated_by | text | |
| target_date | date | |
| created_at, updated_at, status_changed_at | timestamptz | |
| images | jsonb | |
| owner_user_id | uuid | nullable FK to `user_roles.id` (Apr 18 migration). PostgREST embed `select=*,owner_user_id(id,name,email,role)` returns the joined user or null. `owner` text column preserved as the legacy role label. |

NO `comments` column on posts. Never write to it.

**user_roles.role** values are lowercase (`admin`, `creative`, `servicing`, `client`). The posts.owner text column still carries Title-Case labels because of the legacy CHECK constraint (`Admin`, `Servicing`, `Creative`, `Client`); normalise with `normalizeRole()` before writing.

**post_comments** — PK id(uuid). Client-facing. Cols: post_id, author, author_role, message(text), created_at(timestamptz), edited_at(timestamptz), visibility(text), mentioned_users(array), resolved(bool), resolved_at(timestamptz), attachments(jsonb), read(bool), resolved_by, post_title, reply_to(uuid), deleted(bool).

**internal_notes** — PK id(uuid). Agency-only. Same shape as post_comments (including `edited_at` + `resolved_at`). `visibility` gates which agency roles see it.

**post_comment_reactions** — PK id(uuid). Cols: comment_id(uuid), post_id, author, author_role, emoji, created_at.

**user_roles** — PK id(uuid). Cols: email(unique), role(text), name(text). Access + role source of truth.

**requests** — PK id(text, REQ-*). Cols: title, description, created_by, created_at(timestamp), status(text), content_type, target_date(date), images(jsonb), drive_link. Status: `pending` → `assigned` → `closed`. NO `updated_at` column. Pending rows merged into `AppState.posts.all` with `_isRequest:true`.

**notifications** — PK id(uuid). Cols: user_role(text), post_id, type, message, read(bool), created_at, actor. RLS DISABLED. pg_cron `cleanup-old-notifications` deletes > 30 days at 03:00 UTC.

**activity_log** — PK id. Cols: post_id, actor, action, old_stage, new_stage, created_at, read, updated_by. Stage change history.

**audit_log** — PK id. Cols: post_id, action, old_value, new_value, changed_by, changed_at. Caption edit history. RLS unrestricted.

**error_log** — PK id(uuid). Cols: error_message, error_stack, user_email, user_role, page, action, created_at, app_version. Fed by `window.logError`. RLS DISABLED (keep off).

**tasks** — PK id(bigint). Cols: assigned_to, message, due_date, done(bool), created_at. RLS unrestricted.

**profiles** — PK id(uuid). Cols: email(text UNIQUE, FK → user_roles.email), display_name, username(text UNIQUE), title, avatar_url, status(text DEFAULT 'offline'), last_active_at(timestamptz), default_view(text DEFAULT 'dashboard'), timezone(text DEFAULT 'Asia/Kolkata'), notification_email(bool DEFAULT true), notification_digest(bool DEFAULT true), notification_client_comments(bool DEFAULT true), notification_whatsapp(bool DEFAULT false), onboarding_complete(bool DEFAULT false), scratchpad(text DEFAULT ''), created_at(timestamptz), updated_at(timestamptz). Cached client-side in `window._profilesCache` by `12-profiles.js`. RLS: SELECT unrestricted, UPDATE own row only.

**click_log** — telemetry sink (session_id, etc.) fed by `_flushClickBuffer`. Storage: R2 bucket `sorted-images`, CDN `https://images.srtd.io/` (legacy `pub-6a2a4aa8073d454ab9aeee69ef841635.r2.dev`).

## 3 — FILE MAP

28 versioned resources (2 css + 26 js). Includes `/srtd-next/dist/sorted-react.js` (renamed from `/pcs-next/` in B5.5a.4 — scope covers every React rebuild, not just PCS) and `/srtd-next/dist/style.css` (Tailwind bundle added in B5.5a.5-pre). `00-appstate.js` + `00-appstate-compat.js` have NO `defer`. `04-router.js` MUST be the LAST script. `actions/pcs-longpress.js` must load AFTER `actions/pcs.js`. `12-profiles.js` must load AFTER `utils.js` and BEFORE `03-auth.js`. The React bundle is loaded AFTER `ai-config.js` so future React code can read `window.AI_CONFIG`.

Root:
- `00-appstate.js` — AppState brain, logError, onerror, onunhandledrejection, guardAction, _sessionId
- `00-appstate-compat.js` — illegal-access guards around AppState
- `01-config.js` — constants, ROLE_STAGES, ROLE_TABS, `setStage()` (pure stage mutator + logger)
- `02-session.js` — session globals
- `utils.js` — esc(), formatDate()
- `12-profiles.js` — Profile data loader and helpers. Fetches `profiles` table on login, caches in `window._profilesCache` (keyed by email). Also fetches `user_roles` to build `window._nameToRoleCache` (maps names and emails to roles) and `window._nameToEmailCache` (maps short names to emails for avatar/display-name resolution). Exposes: `getDisplayName(email)`, `getAvatarUrl(email)`, `getProfileByEmail(email)`, `getRoleFor(nameOrEmail)`, `updateLastActive()`, `fetchProfiles()`, `enrichAppStateUser()`, `renderAvatar(emailOrName, role, size, opts)`, `_avatarColors(role)`, `openProfilePanel()`, `closeProfilePanel()`, `saveProfile()`, `handleAvatarUpload(file)`, `_renderProfileTrigger()`. All helpers return graceful fallbacks if cache not loaded or fetch failed. `enrichAppStateUser()` sets `AppState.user.displayName`, `.avatarUrl`, `.title`, `.username` from the cached profile after fetch, then calls `_renderProfileTrigger()` to populate the topbar avatar. `getRoleFor()` resolves any name or email to a canonical role string via `_nameToRoleCache`; returns `''` for unknown inputs. Profile panel: full-screen overlay (`#prof-overlay`, z-index 1300) for editing own display name, username, title, notification prefs, and uploading avatar photo. Opens via `openProfilePanel()` (topbar avatar trigger), closes via `closeProfilePanel()`. `saveProfile()` PATCHes `/profiles` and updates local caches + AppState. `handleAvatarUpload(file)` validates type/size (<5MB), compresses via `_compressAvatar` (max 400×400 JPEG q0.80), uploads to R2 `profile-pictures/{sanitizedEmail}.jpeg` (sanitize: @ → -at-, . → -), stores clean URL `https://images.srtd.io/` + key in DB, uses cache-busted `?t=` URL for immediate display, PATCHes avatar_url. Scratchpad: profile panel includes a textarea that auto-saves on blur to the `scratchpad` column; also included in `saveProfile()` PATCH payload. Separately, a full-screen scratchpad overlay (`#scratchpad-panel` in index.html, opened via `openScratchpadPanel()` from the user menu) has NO Save button — it runs Google-Docs-style debounced auto-save: `_scratchOnInput` fires on every keystroke, clears any pending save/retry, and schedules `_scratchPerformSave(false)` 1500 ms after typing stops. `_scratchPerformSave(isRetry)` PATCHes `/profiles` with `{ scratchpad, updated_at }`, drives the `#scratchpad-status` subtle indicator via `_scratchSetStatus(state)` where `state ∈ {'', 'saving', 'saved', 'retrying', 'failed'}` (Saving… grey → Saved grey fade-out after 2s → Save failed — retrying… amber → Save failed red), and on first failure schedules exactly ONE retry 3000 ms later. `closeScratchpadPanel()` flushes any pending debounced save immediately before hiding the overlay so nothing is lost. Globals: `window._scratchSaveTimer`, `window._scratchRetryTimer`, `window._scratchSavedHideTimer`, `window._scratchLastSaved`, `window._scratchInFlight`. Input listener is one-shot wired via `input._autoSaveWired` guard so reopening the panel does not double-wire.
- `03-auth.js` — magic link, OTP, refreshSession (typed errors, 400-is-token-reuse recovery), cross-tab refresh lock, visibilitychange refresh, normalizeRole, single canonical `window._tokenRefreshTimer` (50-min proactive refresh, installed at top of activateRole, torn down in logout / _clearSessionAndLogin)
- `04-router.js` — routing, deep-link handling (must be LAST script)
- `05-api.js` — apiFetch (no-cache headers, 401 retry), uploadPostAsset, logActivity, normalise
- `06-post-create.js` — new post form, asset input, WhatsApp KV preview seed. Caption field (`.npc-caption-wrap` + `#new-post-caption`): inline textarea is manual-only; ⤢ `#npc-expand-btn` opens `openCaptionWorkspace(mode, {postId:null, initialCaption, onUse, onClose})` in detached mode, routing "Use this" back into the textarea via `onUse` instead of PATCHing `/posts`. Word meter `#npc-word-meter` colors 80–100 green / 101–125 amber / 126+ red; cost chip `#npc-cost-chip` reads `window._captionWS.sessionCost` formatted via `window._cwFormatINR` (×100 unchanged). `saveDraft`/`loadDraft` round-trip caption. On submit, fire-and-forget PATCH to `/ai_usage?post_id=is.null&created_by=X&created_at=gte.Y` stamps this session's detached ai_usage rows with the new `post_id`.
- `07-post-load.js` — loadPosts(fromPoll), loadPostsForClient(skipRenderIfUnchanged, fromPoll), mergePosts, startRealtime (installs agency Realtime then wires a 10s polling fallback gated on `window._agencyRealtimeChannel`), startAgencyRealtime/stopAgencyRealtime (agency Supabase Realtime WebSocket — 4 postgres_changes listeners, replaces the 10s poll + the 20s notifBadgeTimer for agency users), _agencyRealtimeRefresh (400 ms debounced loadPosts(true), stashes into _postsPollStash when a modal is open), _onAgencyNotificationInsert, startClientRealtime/stopClientRealtime (client Supabase Realtime WebSocket — 4 postgres_changes listeners, no polling), _initSupabaseRealtimeClient (shared by agency + client), _clientRealtimeRefresh (400 ms debounced loadPostsForClient), _onClientNotificationInsert, _postsFingerprint, _clientPostsFingerprint, _drainPollStash, _renderBackgroundViews, bottom sheets, _cardClickDelegate
- `08-post-actions.js` — quickStage, updatePost, clientApprove, _confirmPublish, _sendStageNotif, _startSaveTimeout/_clearSaveTimeout
- `09-approval.js` — client approval flow, submitApproval
- `09-library.js` — library view (calls `_renderPCS` directly — keep on window.*)
- `10-ui.js` — toasts, notifications panel (v6 redesign — see §4 “Notification panel v6”), switchTab, Action Router, click telemetry flush
- `ai-config.js` — defines `window.AI_CONFIG` (workerUrl, secret, model) — single source of truth for every AI feature
- `/srtd-next/dist/sorted-react.js` — React runtime bundle (strangler-fig). IIFE, non-module, loads after `ai-config.js`. Feature flag `window.ENABLE_REACT_NEW_POST` gates consumption (off until PR B2). See §9.

render/:
- `render/dashboard.js` — dashboard, scoreboard, runway/stage sheets
- `render/client.js` — client feed, comment input, new request overlay
- `render/pipeline.js` — pipeline view, batch mode, chip + person filters
- `render/brief.js` — brief sheet, dynamic assign dropdown, reassign. `_openBriefSheet` is now `async`: on open it awaits `apiFetch('/post_comments?post_id=eq.<pid>&deleted=eq.false&order=created_at.asc')` and renders Section 02 "Discussion" between The Brief (Section 01) and Reference Photos (now Section 03 — Assigned To becomes Section 04). The discussion section uses `_briefBuildDiscussionHtml`, `_briefCommentRowHtml`, `_briefAvatarHtml` (which falls back to `getAvatarUrl(author)` from `12-profiles.js` for profile pictures, then to a role-colored initial badge), and `_briefRoleColor`. Comment submit goes through `window._briefSubmitComment(postId)` — optimistic insert + count badge bump, POST to `/post_comments` with `{post_id, author: AppState.user.name, author_role: normalizeRole(effectiveRole), message, post_title, created_at}`, and rolls back the DOM + badge on failure. Enter key (no Shift) submits, Shift+Enter inserts a newline. `_assignBrief` normalizes the raw person name from `/user_roles?role=eq.creative` through `normalizeRole()` into a canonical DB role (`dbOwner`) before writing to `posts.owner`, because the posts table has a CHECK constraint `posts_owner_check` that only allows `{Admin, Servicing, Creative, Client}`. `ownerName` is still used for display (toast, logActivity, UI). For `_isRequest` posts, `_assignBrief` creates the new post FIRST and only PATCHes the originating request to `status=assigned` AFTER the POST succeeds — so a failed post creation leaves the request pending and visible instead of vanishing. Brief sheet WhatsApp share uses `window._shareBriefOnWhatsApp(postId)` which mirrors `actions/pcs.js _sharePostOnWhatsApp` (location.href + `srtd.io/p/XXXX` short URL) — the old inline `window.open(url, '_blank')` was dropped by iOS Safari's popup blocker on the whatsapp:// scheme handoff.

actions/:
- `actions/pcs.js` — Post Card System full-page overlay. `toggleTaskResolve` writes/clears `resolved_at` timestamp alongside `resolved` + `resolved_by` on post_comments/internal_notes PATCH.
- `actions/pcs-longpress.js` — long-press bottom sheet for PCS comments (wraps openPCS)
- `actions/pcs-claude-caption.js` — Caption Claude Workspace full-screen overlay. Exposes `window.openCaptionWorkspace(mode, context)` and `window._captionWS`. Supports detached mode (`postId:null`) for the Create Post ⤢ flow.
- `actions/pcs-polish.js` — reply polish widget for the PCS composer
- `actions/pcs-select.js` — PCS selection/batch helpers

Other: `index.html`, `styles.css`, `r2-upload-worker.js`, `wrangler.toml`, `package.json`, `vitest.config.js`, `playwright.config.js`, `rollback.sql`, `sql/`, `preview/`, `sorted-preview-worker/`, `mockups/`.

## 4 — ARCHITECTURE RULES AND GOTCHAS

### Plan React realtime spine
- Plan React realtime: vanilla owns Supabase channels; React subscribes to window events `sorted:posts-updated` and `sorted:notifications-updated` via `srtd-next/src/flows/plan/store/realtimeBridge.js`. Sheets/PCS pause updates via `pauseRealtime`/`resumeRealtime`. `mergePosts` (07-post-load.js) dispatches the posts event after every `setAll`; `_recomputeBadgeLocal` (10-ui.js) is the single choke point for the notifications event. Bridge never imports `@supabase/supabase-js` and never opens its own channel.

### Cloudflare cache invariant — `index.html` must NEVER be cached
- `index.html` is the cache-busting anchor for every versioned asset (the 28 `?v=YYYYMMDDx` strings). If Cloudflare serves a stale `index.html`, users get the OLD `?v=` query strings, and browsers happily hand back the OLD cached JS/CSS — deploys ship but no one sees them.
- **Rule (set MANUALLY in Cloudflare dashboard — not in this repo):** dash.cloudflare.com → `srtd.io` → Caching → Cache Rules → Create rule → `URL Path` `equals` `/index.html` → Cache eligibility: **Bypass cache**. Save + Deploy.
- Do NOT encode this rule in `wrangler.toml` or any Worker — the two Workers in this repo (`srtd-r2-upload`, `srtd-og-inject`) don't sit in front of `srtd.io/index.html`; the origin is GitHub Pages via Cloudflare's edge, so the rule lives at the zone level.
- `.github/workflows/cloudflare-purge.yml` purges everything on every push to `main-/-root` as a belt-and-braces fallback, but the Cache Rule is the primary defence — purges race with user traffic, rules do not.
- If the Cache Rule is ever removed by accident, users WILL see stale builds until they hard-refresh. Re-create it immediately.

### AppState shape (`00-appstate.js`)
```
window.AppState = {
  user:   { name, email, role, effectiveRole, previewRole },
  posts:  { all, cached, loaded, source, parked, activityLogs, activityFetched, setAll(newPosts) },
  pcs:    { open, postId, post, editingTarget, closeTimer, pendingComment,
            lightbox:{images,index}, activeMenu, openedFrom },
  ui:     { modalOpen, deferredRender, activeTab, taskFilter, pipelineFilter,
            nrsUrgency, retryCount, retryTimer, realtimeTimer, unreadCount },
  timers: { tokenRefresh, dashDatetime, renderTimer }
}
```

### Mutation rules — never break
- NEVER `posts.all.push()`, `posts.all[i].x=y`, or `posts.all.splice()`.
- Add: `setAll(posts.all.concat([newPost]))`. Remove: `setAll(posts.all.filter(...))`. Update: `setAll(posts.all.map(...))`.
- `mergePosts` (07-post-load.js) uses `Object.assign` in place on each existing post so every module holding a reference sees the update. It SKIPS any post with `_isSaving === true` — never overwrite optimistic state during a poll.
- `AppState.pcs.post` is the SAME object as the row in `posts.all`. Apply server responses via `Object.assign(post, serverRow)`; do NOT replace the reference.

### _isSaving lock + 10-second safety net (08-post-actions.js)
- Before every PATCH: `post._isSaving = true` + `_startSaveTimeout(post, postId)`. On success AND catch paths: `_clearSaveTimeout(post)` then `post._isSaving = false`.
- If a PATCH hangs > 10s (`_SAVE_TIMEOUT_MS`) the timer force-clears the flag, logs `[SAVE TIMEOUT]`, and calls `scheduleRender()`. Force-cleared posts can be saved again immediately. `apiFetch` has no timeout — this is the only indefinite-hang guard. Wired into `quickStage`, `clientApprove`, `_executeStageChange`/`Async`, `updatePost`.

### Client Realtime subscriptions (`startClientRealtime` in 07-post-load.js)
- **No polling.** Replaces the legacy 10s `setInterval` (which was the #1 cause of iPhone-Safari 401/logout churn on the Client role). Installs a single Supabase Realtime WebSocket via `window._supabaseClient = window.supabase.createClient(SUPABASE_URL, SUPABASE_KEY, { auth:{ persistSession:false, autoRefreshToken:false, detectSessionInUrl:false }, realtime:{ params:{ eventsPerSecond:10 } }, global:{ headers:{ Authorization:'Bearer '+token } } })` (`_initSupabaseRealtimeClient`), then subscribes ONE channel (`'srtd-client'`) with four `postgres_changes` listeners: `posts` (all events) → debounced refresh, `requests` (all events) → debounced refresh, `post_comments` (all events) → debounced refresh, and `notifications` (INSERT only, filter `user_role=eq.Client`) → `updateNotifBadge()` direct.
- **Singleton + cleanup.** `window._clientRealtimeChannel` holds the channel; `window._supabaseClient` holds the client. `stopClientRealtime()` calls `_supabaseClient.removeChannel(_clientRealtimeChannel)`, clears both globals, kills the `_clientRealtimeDebounce` timer, and defensively clears the legacy `window._clientPollTimer` slot in case any stale hot-reloaded build still has one running. `stopRealtime()` cascades to `stopClientRealtime()` on logout.
- **Debounced refresh.** `_clientRealtimeRefresh` collapses bursty INSERT/UPDATE events into one `loadPostsForClient(true, true)` call with a 400 ms debounce. Guards (in order): `document.hidden`, `sb_access_token`, `window._isLoadingClientPosts`, active-typing (input/textarea/contenteditable). `fromPoll=true` threads `{ allowLogout:false }` through apiFetch so a transient 401 during the refresh fetch cannot evict the user.
- **Visibility reconnect.** Single `visibilitychange` listener gated on `window._clientRealtimeVisBound`. On `visibilityState==='visible'`, inspects `_clientRealtimeChannel.state` (phoenix channel state: `closed | errored | joined | joining | leaving`). If anything other than `joined`/`joining`, tears the channel down and re-subscribes. **Does NOT poll or fetch posts on focus** — the first INSERT/UPDATE delivered after reconnect triggers the refresh.
- **Notification badge bypass.** The agency-side `AppState.timers.notifBadgeTimer` (10-ui.js, 20 s cadence) skips when `AppState.user.effectiveRole === 'Client'` — the notification Realtime INSERT handler fires `updateNotifBadge()` directly, so the bell is incremental instead of polled.
- **Required Supabase publication setup (run ONCE on the database):**
  ```sql
  ALTER PUBLICATION supabase_realtime ADD TABLE posts;
  ALTER PUBLICATION supabase_realtime ADD TABLE requests;
  ALTER PUBLICATION supabase_realtime ADD TABLE post_comments;
  ALTER PUBLICATION supabase_realtime ADD TABLE notifications;
  ```
  All four tables must be in the `supabase_realtime` publication or `postgres_changes` delivers zero events. RLS is disabled on `notifications` (see §2), so the `user_role=eq.Client` clause is a SERVER-SIDE broadcast filter applied by the Realtime server, not a row-level security policy.
- `loadPostsForClient` still skips overwriting `post.post_comments` when `_commentSaving === true` — object-level lock set by `_handleSubmitComment` (render/client.js) so in-flight optimistic rows aren't clobbered.
- **Invariant:** `_handleSubmitComment` runs `decodeHtmlEntities(input.value)` on the raw textarea value BEFORE deriving `message` / `savedValue` / the POST body — prevents browser-escaped `&#39;` / `&amp;` etc. from round-tripping into `post_comments.message`.
- `renderClientView` guards cv click listeners via `cv._clientEventsWired` (cv persists across re-renders, unguarded `addEventListener` stacks on every render).
- Client @mention roster fetched live from `/user_roles`, cached in `window._clientMentionRoster` via `_fetchClientMentionRoster()`.

### Render pipeline
- `setStage(post, stage)` is a PURE logger — mutates `post.stage`, appends activity log. Does NOT touch `_isSaving`, fire notifications, or render. Safe for rollback.
- `scheduleRender()` DEFERS when `AppState.ui.modalOpen === true` → `window._deferredRender = true`. `_drainDeferredRender()` fires the queued render on modal close + drains the poll stash.
- `_postsFingerprint()` is a cheap hash of `posts.all`. Renderers skip rebuild when unchanged — always mutate via `setAll`. `_renderBackgroundViews()` re-renders dashboard/pipeline/client feed without tearing down PCS. PCS render goes through `_renderPCS()`; `09-library.js` is the one exception.

### Agency Realtime subscriptions (`startAgencyRealtime` in 07-post-load.js)
- **Realtime-first, polling-fallback.** `startRealtime()` (called from the agency branch of `activateRole`) first calls `startAgencyRealtime()` to open a Supabase Realtime WebSocket on channel `'srtd-agency'`, THEN installs the legacy 10 s `window.AppState.timers.realtimeTimer` interval. The interval callback short-circuits (`if (window._agencyRealtimeChannel) return;`) on every tick when the channel is live, so no `/posts` HTTP traffic fires. The interval stays wired as a safety net — if the Supabase JS SDK failed to load or the WebSocket never opens, the 10 s fallback kicks in unchanged.
- **Four postgres_changes listeners on one channel.** Mirrors the client channel shape: `posts *` → `_agencyRealtimeRefresh()`, `requests *` → `_agencyRealtimeRefresh()`, `post_comments *` → `_agencyRealtimeRefresh()`, `notifications INSERT` (filter `user_role=eq.<effectiveRole>` — title-cased) → `_onAgencyNotificationInsert()` → `updateNotifBadge()` direct. Uses the shared `_initSupabaseRealtimeClient()` so agency + client sessions use ONE underlying `window._supabaseClient` (which is idempotent).
- **Debounced refresh + modal-open stash.** `_agencyRealtimeRefresh` collapses bursty events into a single `loadPosts(true)` call with a 400 ms debounce. Guards: `document.hidden`, `sb_access_token`. **Modal-open branch:** when `AppState.ui.modalOpen === true`, fetches `/posts?select=*&order=created_at.desc` and pushes the normalised array into `window._postsPollStash` + `_postsPollStashFp` — does NOT call `loadPosts()`, `mergePosts()`, or `scheduleRender()`, exactly matching the legacy poll's stash contract so every existing modal-close drain site (see Agency drain-stash contract below) keeps working without changes. **Modal-closed branch:** calls `loadPosts(true)` which threads `{ allowLogout:false }` through the posts + requests + comment-counts fetches, skips `showLoadingSkeleton`, silences the success toast, and silences error banners on realtime-driven refreshes.
- **Visibility reconnect.** Single `visibilitychange` listener gated on `window._agencyRealtimeVisBound`. On `visibilityState === 'visible'`, inspects `_agencyRealtimeChannel.state`; if not `joined`/`joining`, tears the channel down and re-subscribes. Does NOT fetch or poll on focus — the first INSERT/UPDATE delivered after reconnect triggers the refresh through `_agencyRealtimeRefresh()`.
- **Notification badge gate.** `AppState.timers.notifBadgeTimer` (10-ui.js, 20 s cadence, preserved for the defensive-guards test) short-circuits when `window._agencyRealtimeChannel` is set, in addition to the Client guard from PR #826. Cadence is unchanged at 20 000 ms so the `notifications.test.js` regex `AppState\.timers\.notifBadgeTimer\s*=\s*setInterval[\s\S]*?,\s*20000` still matches. Falls through to the REST badge call only if the agency Realtime channel never established.
- **`stopRealtime()` cascades** to both `stopAgencyRealtime()` and `stopClientRealtime()` on logout / `_clearSessionAndLogin`. Both realtime teardown calls are idempotent.
- **Required Supabase setup:** the four ALTER PUBLICATION commands are the same as the client Realtime PR — already applied on the database. No new ALTER TABLE needed for this PR.
- **Realtime liveness:** `window._realtimeLastEventAt` stamped on every postgres_changes event; 60 s watchdog triggers `stopRealtime` + `startRealtime` + catch-up fetch if `channel.state='joined'` but no event in 180 s.

### Agency drain-stash contract (`_postsPollStash` in 07-post-load.js)
- Writers: `_agencyRealtimeRefresh()` modal-open branch (Realtime path, primary), the 10 s `startRealtime()` poll fallback modal-open branch (legacy path, only active when Realtime dropped).
- Reader: `_drainPollStash()` reads the stash, clears it, runs `mergePosts(stash)`, `scheduleRender()`, `updateNotifBadge()` — no-op when stash is null.
- Trigger: `_drainDeferredRender()` calls `_drainPollStash()` unconditionally after its `safeRender` debounce, so every modal-close drain site also flushes stash data. Drain sites: `forcePCSReset` (actions/pcs.js), `closeAdminEdit` / `closeNewPostModal`, `closeNotifications` / `closePipelineFilter` (10-ui.js), `_lbClose` and client post overlay close (render/client.js).
- Latest-wins, no queue — stash is cleared before `mergePosts` runs.
- Client Realtime path has NO stash — `startClientRealtime` never fetches posts during a modal; active-typing guard + `loadPostsForClient(true, true)` fingerprint check handle all refresh cases.

### Runtime-enriched fields (NOT in DB — added by loadPosts)
- `_commentCount` (int), `_clientCommentAt` (ISO|null) — set after batch comment fetch. `_isRequest` (true) — set on rows merged from `requests`; brief/assign/close/reopen branch on this to route `/requests` vs `/posts`. `_isSaving` (bool) + `_saveTimer` — optimistic save lock. `_commentSaving` (bool) — set by `_handleSubmitComment` during an in-flight insert; `loadPostsForClient` honours it so the optimistic row isn't clobbered.

### Design system
- Zero border-radius on inputs/buttons. NO `rgba()` — use 8-digit hex (#RRGGBBAA). Approved exceptions: `.pcs-more-ov`, `.pcs-more-l`, SVG fill at index.html:1140.
- Colors: bg `#080808`, surface `#0d0d12`, comments bg `#191924`, text `#E8E8E8`/`#AEAEB2`/`#8E8E93`, gold `#C8A84B`, red `#FF4B4B`, green `#3ECF8E`, purple `#9b87f5`, cyan `#22D3EE`, amber `#F6A623`. Owners: Chitra `#22D3EE`, Pranav `#9b87f5`, Client `#FF4B4B`, Shubham `#C8A84B`.
- Fonts: IBM Plex Mono (labels/meta) + DM Sans (body/UI). Dark theme default.
- Use `100dvh`, never `100vh` (iOS Safari address bar).
- Image compression: posts max 1200px q0.82, comments max 800px q0.80, save as .jpg.

### Notification panel (10-ui.js `renderNotifications`)
- `loadNotifications`, `updateNotifBadge`, `markNotifRead`, `markAllNotificationsRead`, `deleteNotification`, `openNotifications`, `closeNotifications` stay stable — tweak visuals in `renderNotifications` / `_buildItem` only.
- Header: mono `<effectiveRole> · SORTED` label + plain-text `Mark read | Close`; greeting row `Hey, <AppState.user.name>` (name gold). Name/role pull ONLY from `AppState.user` — no hardcoded map.
- Tabs (`.notif-chips > .notif-chip`): text-only (All / Mentions / Comments / Moves), counts in `nchip-count-*` spans. Active class reapplied by the renderer on every pass.
- Cards: `.notif-item` is the single card class; published rows get `notif-live-card` as a marker so the tap delegate `closest('.notif-item, .notif-live-card')` still resolves. Background always transparent — read and unread items look identical. Inline `<span class="nnew-dot">` (gold 5px) before the actor name is the ONLY unread indicator. Avatars solid-fill 32px (no border/ring). Published cards render a `.notif-pub-label` eyebrow (`✓ PUBLISHED`) above the main text line.
- Meta row (`.notif-meta`): flex with `gap: 10px`. Layout is `time [LinkedIn] [chip] <.meta-actions>WA trash</.meta-actions>`. LinkedIn sits AFTER the timestamp (not inside `.meta-actions`). `.meta-actions` only wraps the WA + trash icon buttons with an internal `gap: 2px` and `margin-left: auto`, so the icon cluster is always flush-right. No `.notif-meta-sep` dot spans are emitted anywhere — the class is kept as a no-op `display:none` so stale HTML never breaks layout. `.notif-mi-btn` is a 28×28 rounded-6px tap target with `background: #FFFFFF0A` on hover. SVG children are `pointer-events:none`. Panel tap delegate skip list includes `.notif-mi-btn`, `.notif-topbar-btn`, `.notif-li-link`, `.meta-actions`, `.notif-thumb-wrap`, and every thread-view control below. Tapping `.notif-thumb-wrap` ALWAYS navigates to PCS / the client overlay — it never expands the thread drawer, even on comment/mention cards.
- **Expand-in-place thread view** — `renderNotifications` precomputes `_threadCountMap = { [post_id]: non-resolved comment count }` from `window._notifComments` before any `_buildItem` call. Both `'comment'` AND `'mention'` notifications participate in the grouping (`commentGroupMap`) and are expandable. `_buildItem` gates the chip on `(type === 'comment' || type === 'mention') && _threadCountMap[post_id] > 1` and labels the chip with the map value — any post with 2+ visible comments gets a chip regardless of how the notifications were bucketed. Tap on the card body OR the chip toggles `.notif-item.expanded`, mounts the thread HTML, anchors scroll, and marks the notif read on first expand. State persists across `scroll.innerHTML` rebuilds via `window._notifExpandedSet` (Set of notif IDs). Thread HTML is cached per post in `window._notifThreadCache` (cleared at the top of `loadNotifications`).
- Drawer content: `_notifBuildThreadHtml` filters `window._notifComments` by `post_id === n.post_id && !resolved`, sorts DESC by `created_at`, slices to the newest 5, then reverses so display order is chronological within the 5. If `totalCount > 5` a `.thread-overflow` "View all N comments" link is prepended to the drawer and routes through the same open-post handler as `.thread-open-post` (close panel → PCS / client overlay). Each message renders through `_notifThreadMsgHtml` with thread-specific classes (`.thread-msg`, `.thread-av`, `.thread-author`, `.thread-role`, `.thread-time`, `.thread-message`) that are deliberately smaller and tighter than the notification card classes so the drawer reads as a nested conversation, not as more notifications.
- Drawer visuals: `.thread-area` is `#1C1926` + `border: 1px solid #C8A84B1F` + `border-radius: 8px` + `padding: 6px 10px`. `.notif-item.expanded` uses `background: #16161F` + `border-left: 3px solid #C8A84B4D` + `border-radius: 10px` + `padding-bottom: 0` + `margin-bottom: 8px`. `.notif-item.expanded .thread-drawer` uses `max-height: 3000px` + `opacity: 1` so the reply row + "Open full post →" link never clip. `.thread-reply` carries `border-top: 1px solid #C8A84B1A` + `padding: 6px 0 4px` + `margin-top: 4px`. Ultra-compact thread typography (deliberately smaller than notification cards): `.thread-av` 18×18 with `font-size: 7px` on `#000000D9` ink, `.thread-author` 10px, `.thread-role` 7px pill on `#FFFFFF12`, `.thread-time` 8px `#FFFFFF33` in `DM Mono`, `.thread-message` 11px `#FFFFFFA6` line-height 1.3, `.thread-msg` `padding: 4px 0` + `gap: 6px`.
- The drawer contains a `.reply-input` + `.reply-send` row and an `Open full post →` footer link. Replies POST to `/post_comments` ONLY — the JS fan-out is intentionally skipped so `notify-comment` (edge) is the single writer and there are no duplicate notification rows. Reply author is always `AppState.user.name`, author_role is `effectiveRole` title-cased, so the same code path works for both agency and client users. The panel tap delegate skips `.expand-chip`, `.reply-input`, `.reply-send`, `.thread-msg`, `.thread-area`, `.thread-reply`, `.thread-footer`, `.reply-label`, `.meta-actions`, and routes `.thread-open-post` / `.thread-overflow` clicks through `closeNotifications()` + `openPCS` / `_openClientPostOverlay`.
- Tests: structural class names + source patterns (`notif-item`, `notif-live-card`, `nchip-count-*`, grouped key `(n.post_id||'') + '|' + (n.actor||'')`, `notif-resp-time`, day labels, `"left " + n + " comments on "`) are preserved verbatim. 508/508 unit tests still pass.

### Session resilience (05-api.js + 03-auth.js) — 401 hardening
- `apiFetch(path, options, meta)` — optional third `meta` arg. `meta.allowLogout === false` disables the `_clearSessionAndLogin()` call on the `auth_expired` branch so a transient 401 in a background poll CANNOT evict the user. Only user-initiated actions (comments, stage changes, approvals) get the default `allowLogout: true` behaviour.
- Background pollers that pass `{ allowLogout: false }`: `updateNotifBadge` (10-ui.js, 10s interval), `_flushClickBuffer` (10-ui.js, 5s interval), `startRealtime` both 5s agency poll branches (07-post-load.js), and `loadPostsForClient(true, true)` called from `startClientRealtime` (5s client poll). `loadPostsForClient` accepts a new `fromPoll` boolean; when truthy it builds `_apiMeta = { allowLogout: false }` and threads it through every internal apiFetch call (posts, requests, post_comments). User-facing first-render calls leave `fromPoll` unset so the flag stays off.
- `apiFetch` second-chance refresh: after the first `refreshSession()` returns `{error:'server'|'network'}`, apiFetch now waits 1500 ms and retries `refreshSession()` one more time. On success it replays the original request; on failure it falls through to the pre-existing soft-banner + throw. `auth_expired` classification from the retry is honoured.
- `window._tokenRefreshTimer` — SINGLE canonical 50-minute proactive refresh installed at the TOP of `activateRole()` (03-auth.js) so every branch (admin, agency, admin-preview, client) gets it before any early return. The prior duplicates (`AppState.timers.tokenRefresh` in startRealtime and `window._clientTokenTimer` in the client branch) were deleted — one owner, one mental model. Torn down in `logout()` and `_clearSessionAndLogin()`.
- `_doRefresh` (03-auth.js) narrows the `auth_expired` classification: HTTP 400 from `/auth/v1/token` is treated as a refresh-token REUSE race (another tab rotated first) and is RECOVERED by returning the currently-stored `sb_access_token`. HTTP 401/403 only map to `auth_expired` when the response body carries a `refresh_token_not_found`, `refresh_token_already_used`, `refresh_token_expired`, or `invalid_grant` code. Any other 401/403 is returned as `{ error: 'server' }` so the second-chance refresh in apiFetch (Fix 5) gets a chance. This is the fix that stops multi-tab users from getting evicted during legit token rotations.
- `sendMagicLink` catch branch now classifies `TypeError` as a network failure and shows "Unable to reach the server. Check your connection and try again." instead of Safari's raw "Load failed" string. Rate-limit errors containing "31 seconds" get a friendly "Please wait a moment before requesting another code." message.

### Misc gotchas
- `_commentInputHtml()` only renders for `awaiting_approval`/`awaiting_brand_input` and MUST be called inside `_cardHtml()`. `apiFetch()` never calls `logout()` on 401 by design — refresh flow handles it. Silent `.catch(()=>{})` is a bug — always `window.logError`.
- Timers: **10s agency `startRealtime` poll is a realtime-fallback only — short-circuits on `window._agencyRealtimeChannel`**. **20s agency notifBadgeTimer is also a realtime-fallback — short-circuits on `window._agencyRealtimeChannel` OR Client effectiveRole**. 5s click-log flush (write batcher, unchanged). 50min token refresh (auth, unchanged). **Both Client and Agency roles use a Supabase Realtime WebSocket as the primary data path; the legacy intervals are kept wired as cold-start + disconnect safety nets only.** Client DB role takes priority over `pcs_role_preview`. Deep link `srtd.io/?open=POST_ID` → `window._pendingOpenPost` fired after loadPosts. The deep-link drain handler in `07-post-load.js` (renderAll, ~line 1054) is stage-aware: brief / `_isRequest` rows route to `window._openBriefSheet(_pid)`; client effectiveRole routes to `window._openClientPostOverlay(_pid)`; everything else opens PCS via `openPCS(_pid)`. This mirrors the `_cardClickDelegate` so deep links never land in the wrong overlay.
- Pipeline brief chip: `index.html#stage-strip` includes a `<div class="stage-chip brief" data-stage="brief" style="display:none;">` chip with `chip-count-brief`. `render/pipeline.js updatePipelineChipCounts` writes the count and toggles visibility (>0 shows, =0 hides). `dotColors.brief = '#C8A84B'` provides the gold marker. Pipeline search (`handlePipelineSearch`) and the pipeline filter sheet (`openPipelineFilter` + `_pfChip` in 10-ui.js) both include `'brief'` in their stage arrays and label/color maps so brief cards are searchable and filterable from the strip overlay.
- Notification tap routing: `10-ui.js _buildItem` sets `data-is-brief="1"` on `n.type === 'new_request' || n.type === 'brief'`. The notification panel tap delegate uses that flag to call `_openBriefSheet(_tPid)` instead of `openPCS(_tPid)` for the matching rows.
- PCS document-click-close skips close when the active dropdown contains `input[type="date"]`. `_cardClickDelegate` (07-post-load.js) guards `input, textarea, button, [contenteditable="true"], a, [role="button"]` — don't narrow.
- Image download: `_pcsLbDownload` + `_pcsSaveAllPhotos` must strip BOTH `https://images.srtd.io/` and legacy `pub-*.r2.dev` prefix before hitting the R2 worker `/download?key=`.

## 5 — AUTH AND ROLES

DB roles (canonical, Title Case): `Admin`, `Servicing`, `Creative`, `Client`. Person names NEVER go to the DB.

- `effectiveRole` = the canonical DB role the app acts as, possibly overridden by admin preview.
- `normalizeRole(x)` canonicalizes any person name / casing to a DB role.
- Client activation: skips `startRealtime()` and instead calls `startClientRealtime()` (Supabase Realtime WebSocket — no polling; see §4).
- Admin preview: set `AppState.user.previewRole` (stored as `pcs_role_preview` in localStorage) to render as another role without touching the DB. A real Client DB role ALWAYS wins.
- `switchTab(tabOrEl)` accepts a string (`'dashboard'`, `'pipeline'`, `'tasks'`, `'client'`) OR a DOM element from click delegation. The `pipeline` branch triggers `loadPosts()` with a `_isFetchingPosts` guard to prevent double fetches on rapid tab spam.
- Sessions: `refreshSession()` returns typed errors: `{token}` | `{error:'auth_expired'|'server'|'network'}`. Network errors KEEP tokens. Cross-tab refresh lock via `localStorage._srtd_refresh_lock` prevents Supabase token-reuse revocation. `visibilitychange` listener guarded by `window._authReady` refreshes on tab focus.
- Magic-link flow clears stale `sb_access_token` BEFORE writing the fresh token.

## 6 — EDGE FUNCTIONS AND WORKERS

Edge functions live in Supabase — NOT in this repo. Do not try to edit them from here. "View Post" links use `https://srtd.io/?open=${postId}`.

- **notify-stage** — fires on stage change. Fans out email + in-app notification per the routing matrix (Admin / Servicing / Creative / Client).
- **notify-comment** — fires on `post_comments` insert. Notifies mentioned users + subscribed roles.
- **notify-request** — fires on `requests` insert. Notifies Admin + Servicing with brief content.
- **notify-digest** — scheduled daily digest email per role listing pending items.

Cloudflare Workers:
- **srtd-og-inject** — route `srtd.io/preview/*`. Source: `sorted-preview-worker/src/index.js`. Serves WhatsApp OG HTML from KV `sorted-whatsapp-previews`. Previews seeded fire-and-forget by `06-post-create.js` and `render/brief.js` POSTing to `/generate-preview`. Lookup `?id=POST_ID` (direct) or `?p=SLUG` (legacy). Shared secret `PREVIEW_SECRET=srtd2026xK9mN3pQ`. Short URL: `srtd.io/p/XXXX` via Cloudflare Page Rule. Zero Supabase egress. **Invariant:** `handlePreview` checks `env.PREVIEWS_KV.get(shortId)` first and, on a cold Supabase-backed miss, populates KV via `ctx.waitUntil(env.PREVIEWS_KV.put(shortId, html, { expirationTtl: 2592000 }))` so the next hit never re-queries Supabase.
- **srtd-r2-upload** — `srtd-r2-upload.ksg-kumarshubhamgune.workers.dev`. Handles uploads and `/download?key=` (sets `Content-Disposition: attachment`). Source: `r2-upload-worker.js`.

ai_usage lifecycle invariant (`srtd-ai-worker/src/index.js`): every Anthropic call writes a `pending` row (with `estimated_cost_usd` from a `Math.ceil(promptChars/4) * USD_PER_INPUT_TOKEN` estimate) BEFORE firing, then PATCHes the row to `status='complete'` (with real `tokens_input`, `tokens_output`, `actual_cost_usd`, and `cost_usd` mirrored to `actual_cost_usd` for back-compat) on success or `status='failed'` on throw / non-200. INSERT failure logs to `console.error` and proceeds without a row id; UPDATE failure logs and still returns the response. `/ai/month-cost` is now a stub returning `{ success: true, month_cost_inr: null }` — month spend comes from the `ai_usage` table client-side.

File-upload invariant (`srtd-ai-worker/src/index.js`): `POST /ai/upload` (multipart, X-AI-Secret gated) stores a PDF or image (`application/pdf` + `image/jpeg|png|webp|gif`, 10 MB cap) in R2 bucket `sorted-ai` (binding `AI_ASSETS`) under key `uploads/<workspace_id>/<post_id>/<ts>-<sanitised-name>`; the returned `key` is passed back as `file_key` + `media_type` in a subsequent `/ai/complete` body, fetched from R2, base64-encoded, and injected as a `document` (PDF) or `image` block into the FIRST user message's content array (R2 fetch failure is logged and swallowed — the AI call proceeds without the attachment). `ai_usage.file_key` (nullable text, migration `sql/006-ai-usage-file-key.sql`) records the attached key on the pending row. `useWebSearch` is now hardcoded `false` across every feature.

product_knowledge table = on-demand product specs fetched by product_name or aliases. Only loaded for writer + chat features. Never loaded for qc, angles, email_brief.

R2: bucket `sorted-images`. CDN custom domain `images.srtd.io` (preferred). Legacy `pub-6a2a4aa8073d454ab9aeee69ef841635.r2.dev` still works.

Email: Resend, FROM `hinglish@srtd.io`.

## 7 — DEPLOY RULES

1. Bump ALL 28 `?v=YYYYMMDDx` strings in `index.html` together (2 stylesheets + 26 scripts — the Tailwind bundle `/srtd-next/dist/style.css` joined the roster in B5.5a.5-pre). Current: `?v=20260422a`. The Supabase JS SDK `<script>` tag sits ABOVE the versioned block and is pinned to an external jsDelivr URL — do NOT add a `?v=` to it.
2. After every merge: Cloudflare dash → srtd.io → Caching → Purge Everything. Hard refresh every device.
3. Deploy path: merge PR → GitHub Pages publishes from `main-/-root` branch.
4. One PR at a time. TDD mandatory. Never raw `fetch()` — always `apiFetch()`.
5. CLAUDE.md updates in the SAME PR as the code change. Never a separate PR.
6. Every push response must include: files changed, test count, version bump, PR URL.

## ONE PR RULE — NON-NEGOTIABLE
Before writing any fix prompt or creating any branch, always run:
  gh pr list --state open --base main-/-root
If ANY open PR exists, stop immediately and tell Shubham:
  "PR #[number] ([title]) is still open. Merge or close it before I create a new one."
No exceptions — not for critical fixes, not for small changes, not for hotfixes.
One open PR maximum at all times.

## PR CONFLICT GUARD — NON-NEGOTIABLE
Before creating any branch:
1. Run: git fetch origin && git diff --name-only origin/main-/-root HEAD
2. If any file in the planned PR was modified in main-/-root since the last fetch, stop and report the conflicting files. Do not proceed until Shubham confirms.
3. Never branch off anything except origin/main-/-root directly.
4. Never bump ?v= version strings to a letter already used by an open PR. Read open PRs first, then pick the next unused letter.
5. If in doubt about conflict state, run a dry-run merge check: git merge --no-commit --no-ff origin/main-/-root, then git merge --abort. Report any conflicts before touching a single file.

## 8 — TESTS

Unit: `npx vitest run` — 563/563 passing across 22 test files. E2E: `npx playwright test` — 9 specs (3 critical run in CI only on core-logic file changes). Smoke: `.github/workflows/smoke.yml` runs `live-smoke-schedule.spec.js` every 30 min against production. Required secrets: `SORTED_CLIENT_EMAIL`, `SORTED_ADMIN_EMAIL`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `EXPECTED_VERSION`.

## 9 — REACT MIGRATION (strangler-fig)

`/srtd-next/` hosts the React runtime, vendor-isolated from the vanilla repo root. Renamed from `/pcs-next/` in B5.5a.4 because the folder scope covers every React rebuild, not just PCS. Feature flag: `window.ENABLE_REACT_NEW_POST` (inline in `index.html` before any deferred script runs). Load order: bundle ships after `ai-config.js` in `index.html`, with `?v=` bumped in lockstep with the other 26 versioned assets. `/srtd-next/` has its own `package.json` + `node_modules/` isolated from root Vitest deps. Build: `cd srtd-next && npm run build` produces `dist/sorted-react.js` (IIFE, esbuild-minified, React prod path via `process.env.NODE_ENV` define in `vite.config.js`); `dist/*.map` is gitignored. Status after B5: Import Brief wired for 3 sources — Paste (all roles, opens `PasteSheet.jsx` → appends pasted text to `form.internalNotes` with a `\n\n---\n\n` separator), Gmail (Admin only, opens `GmailSheet.jsx` → bridges through `srtd-next/src/core/bridges/gmail.js` to Worker `/gmail/list` + `/gmail/brief`, prefills title/caption/internalNotes with a Replace-All vs Append-Notes-Only confirm when existing content is present), Upload-a-file (Admin stub only, toasts "Coming soon — File parse endpoint pending Worker PR"). `Sorted briefs` option dropped; pipeline is the canonical brief→post path. The Import pill is now visible to every role (the dropdown itself filters options by `useIsAdmin()`). The 6-dropdown outside-click bug was fixed by deleting `onClick={e=>e.stopPropagation()}` on the modal container in `CreatePost.jsx` — clicks inside the modal now bubble to `document`, so Import/Owner/Stage/Pillar/Format/Location dropdowns all close on any outside tap. B4 admin guards still in place: ⤢ Caption Workspace, cost chip, and 600 ms cost poll only render for admins; word counter universal. Draft persistence unchanged (`localStorage['sorted_create_post_draft_v1']`, 800 ms debounced save on form.update, hydrated at flow open, cleared on submit). B5.5a: admin Paste now opens Caption Workspace instead of writing to Internal Notes — `PasteSheet.handleUse` branches on `useIsAdmin()`. Admin path calls `openCaptionWorkspace('write', { postId:null, syntheticContext:{ source:'create-post-paste', brief:<pasted text>, title, pillar, location, format }, onUse, onClose })` (as of B5.5a.4 this bridge routes to the pure-React `CaptionWorkspace` mounted in `App.jsx`, not the vanilla overlay). Vanilla `actions/pcs-claude-caption.js` write-mode now reads `context.syntheticContext.brief` and — if set — builds the Claude prompt from that brief (falls back to the pre-B5.5a prompt when absent, so existing callers stay unchanged). Both ⤢ and Paste-admin `onUse` now `window.confirm()` before overwriting a non-empty caption. Diagnostic: `CaptionField.handleOpenWorkspace` logs `[react/caption] ⤢ tapped` to console, and `window.__lastCaptionFieldIsAdmin` is set on every CaptionField render so "⤢ not opening" reports can be narrowed to role-guard vs. code-path. Non-admin Paste remains "Use this" → internal_notes append. B5.5a.1: fixed the Anthropic 400 that was killing every AI call since the Messages API tightened role validation. Client fix at `10-ui.js:220` (_claudePolish) drops the `{role:'system'}` entry from the messages array and prepends the polish instruction to the user-content preface instead. Worker at `srtd-ai-worker/src/index.js:305-321` now defensively strips any role !== 'user'|'assistant' from messages and appends their content to the system prompt — so a future regression can't reach Anthropic. `/gmail/brief` schema extended: Claude now returns `content_pillar` (7 GBL enum or null), `target_date` (ISO YYYY-MM-DD or null, never invented), `location` (5 GBL sites or null), and `format` (5 UI options or null); `GmailSheet.applyBrief` writes all four on `replace` via `_pillarToUi` normalisation. AI failures now surface: `sendCaptionMessage` in `actions/pcs-claude-caption.js` toasts + logError on `!result.success`; `_claudePolish` gains a `.catch` that does the same; React Footer/Gmail/PasteSheet paths push `logError` via a new `srtd-next/src/core/bridges/logging.js` shim. Every React Create Post event (FAB tap, import open, option tap, workspace open, paste use, gmail list/brief, overwrite confirms, draft-banner choice, clear-form, submit success/failure) now pushes into `window._clickBuffer` — shared with the vanilla 30-s flush at `10-ui.js:23` — so `click_log` has full React coverage without a new network path. Draft restore UX: `createPostFlow.open()` stashes any non-empty saved draft as `pendingDraft` and renders a banner with Restore / Start fresh / ✕ instead of auto-populating the form silently; new Clear button in the Footer (between Cancel and Create Post) wipes the form + draft on confirm. `window.SortedReact.version === '0.7.2'`. 553/553 unit tests still pass; vanilla `openNewPostModal()` remains wired as the FAB fallback. Post-merge the srtd-ai Worker must be redeployed via Cloudflare dashboard so the defensive filter + /gmail/brief schema land in production. B5.5a.4: `pcs-next/` → `srtd-next/` via `git mv` (history preserved); every repo reference updated (`index.html` script src, this `CLAUDE.md`, `srtd-next/README.md`). New folder `srtd-next/src/shared/caption-workspace/` holds a pure-React Caption Workspace cloned from `actions/pcs-claude-caption.js` — four files: `CaptionWorkspace.jsx` (full-screen fixed overlay, z-index 9600, slide-up 280 ms `translateY(100%) → 0`, warm-ink bg `#1A1816`, Fraunces drafts, IBM Plex Mono labels, max-width 480 centered, mobile-first), `store.js` (Zustand — `isOpen`, `mode`, `context`, `messages`, `isSending`, `sessionCost`, `onUse`, `onClose`, `open/close/sendUserMessage/applyDraft/_send`), `api.js` (wraps srtd-ai Worker `/ai/complete` via `getAIConfig()`, `calcCostINR` + `formatINR` helpers mirror the vanilla cost math at USD×100), `systemPrompts.js` (`buildWritePrompt` — brief-first, falls back to title + pillar + existing caption). v1 MVP scope covers only `write` (generate 3 numbered options — `splitOptions` parses `1. / 2. / 3.` into separate cards with per-option Use-this) and `chat` (iterative user/assistant thread); Polish / QC / Rewrite / `ai_memory` read+write are explicitly deferred. The bridge `srtd-next/src/core/bridges/captionWorkspace.js` now calls `useCaptionWorkspaceStore.getState().open(mode, opts)` directly; `getSessionCost()` reads from the React store, so Create Post's 600 ms cost-chip poll in `CaptionField.jsx` updates live while the React workspace is open. `App.jsx` mounts `<CaptionWorkspace />` at body-level alongside `<CreatePost />` — no DOM dependency on `#pcs-overlay` or `#caption-workspace-overlay`. Vanilla `actions/pcs-claude-caption.js`, vanilla `window.openCaptionWorkspace`, and `index.html#caption-workspace-overlay` are 100% untouched — PCS Polish / QC / Rewrite keep using them unchanged. `window.SortedReact.version === '0.8.0'`. All 27 `?v=` strings bumped `20260418m → 20260418n`. 553/553 unit tests still pass. No new deps, no Worker changes, no SQL changes. Branch: `claude/b5-5a-4-srtd-next-react-workspace`. B5.5a.5-pre: Tailwind CSS v3 installed in `/srtd-next/` as a dev dep (`tailwindcss`, `postcss`, `autoprefixer` — no new runtime deps). Design tokens bound to CSS custom properties (`--c-bg`, `--c-terracotta`, etc.) declared in `src/styles/tailwind.css` inside `@layer base { :root }` for a warm-paper light palette and `@media (prefers-color-scheme: dark) { :root }` for the warm-ink palette that matches vanilla exactly; `tailwind.config.js` binds `darkMode:'media'` + `colors.bg = 'var(--c-bg)'` etc. so every utility (`bg-bg-2`, `text-text-mid`, `border-border-warm`, gradient `bg-terracotta-grad`, etc.) auto-flips with the user's system setting. **Preflight DISABLED** — vanilla shares the page and a global reset would break every vanilla view. `src/core/theme/index.js` mirrors the tokens as `var(...)` references plus `getResolvedColor(name)` which reads the active computed value for canvas/chart/SVG-fill contexts that can't consume `var()`. Tokens + fonts + cost helpers exposed at `window.SortedReact.theme`. Vite emits two outputs now: `dist/sorted-react.js` (unchanged shape) and new `dist/style.css` (4.71 KB minified, 1.34 KB gzipped). `index.html` loads both with matching `?v=` — the 27-string roster grew to 28 (2 css + 26 js). Vanilla app stays dark-only until migrated; temporary mismatch on light-scheme iPhones during the strangler-fig transition is intentional and documented in `srtd-next/DESIGN_SYSTEM.md`. No runtime theme toggle — system preference only. Existing 11 create-post components + shared/caption-workspace still render via inline styles from `src/core/tokens.js`; both modules coexist until each component is migrated one-by-one with visual verification. `DESIGN_SYSTEM.md` lives at `/srtd-next/DESIGN_SYSTEM.md` — light+dark token table, fonts, patterns, rules — single reference for every future React PR. `window.SortedReact.version === '0.8.1'`. All 28 `?v=` strings bumped `20260418n → 20260418o`. 553/553 unit tests still pass. No vanilla behaviour change.

PCS React sandbox lives at `/srtd-next/src/flows/pcs/`. Flag-gated via `window.ENABLE_REACT_PCS` which activates when URL contains `?pcs_react=1`. Auto-opens the specified post when `?pcs_post=POST-xxx` is also present. Overlay z-index 1501, lightbox z-index 1600. Does not touch vanilla `/actions/pcs.js` or any vanilla `openPCS` call site. PR 1 shipped: full read-only surface with wired WhatsApp share, Copy link, photo lightbox, tab switching, comment read-more expand, Supabase Realtime subscription via `window._supabaseClient` bridge. Per-section retry for comments/internal-notes on network failure. Schema read uses `posts` embed `owner_user_id(id,name,email,role)` from the Apr 18 FK migration and falls back to the legacy `posts.owner` text when the FK is null. New tokens: `stage-brief`, `stage-production`, `stage-ready`, `stage-input`, `stage-scheduled`, `role-creative`, `role-client` (light + dark pairs in `src/styles/tailwind.css`). New primitives `Overlay`, `Avatar`, `WhatsAppIcon` land in `core/ui/`. `window.SortedReact.version === '0.9.0'`. All 28 `?v=` strings bumped `20260418o -> 20260418p`. No vanilla behaviour change when flag is absent. PR 2 ships all writes: stage move, property edits, caption edit + AI, photo upload/remove, comment create/reply/resolve/react/task, long-press menu, post hard delete.

PR 2 shipped: all PCS writes (stage move with publish variant, owner reassign writing owner_user_id, target date, format/pillar/location dropdowns, title edit, caption edit with audit_log, drive/canva link add/edit/remove, internal_notes edit, client_feedback edit, photo upload+remove, post hard-delete admin-only). Comment composer with @mention autocomplete, image attach, polish preview, reply quote bar, cmd-return send. Comment actions wired: like, reply, resolve/unresolve, soft-delete via long-press. Caption AI integrated via shared caption-workspace with 3 modes added: 'qc' seeds caption + auto-runs review; 'rewrite' passes comments + one-shot rewrite; 'write-options' vanilla 3-options pattern alongside existing angles-first 'write'. Rewrite-from-comments feature fixed to map to Worker feature 'chat' (was 'writer'). Inline comment polish via bridges/claudePolish.js mirrors vanilla _claudePolish at 10-ui.js:226. OG preview reseed fires after caption/image changes via bridges/ogPreview.js. All 28 ?v= strings bumped 20260418x -> 20260418y.

**Invariant:** `post_comments.attachments` can arrive JSON-encoded TWICE (a JSON string whose parse yields another JSON string). `srtd-next/src/flows/pcs/utils/attachments.js normalizeAttachments()` runs `JSON.parse` in a loop up to two times before returning — never collapse it back to a single parse. Reaction writes go through `addReaction(commentId, emoji, createdBy)` / `removeReaction(commentId, emoji, createdBy)` where `createdBy` is the user's email, stored in `post_comment_reactions.created_by` (the legacy `author` / `author_role` columns are no longer written from React).

PR 2.2 shipped: force full-screen takeover for every PCS field editor. New `FullScreenEditor` primitive at `srtd-next/src/flows/pcs/components/FullScreenEditor.jsx` (fixed inset-0, z-index 2600, slide-up keyframe translateY(100%) -> 0, sticky topbar with X close + title + optional Save, max-w 430 centered, pb-safe-b, body-scroll lock, Esc closes). `PropertySheet`, `OwnerSheet`, `StageSheet` all rebuilt on top of it; bottom-sheet `fixed inset-x-0 bottom-0 rounded-t-card` pattern deleted from all three. `CommentActionSheet` (long-press contextual menu) stays as a bottom sheet because it is not a field editor. Title inline edit: the separate "Edit title" pencil row in PCS.jsx is deleted; Topbar title span became a `<button>` that calls `onEditTitle` when `canEdit` is true. Native iOS date picker: `PropertySheet` date branch now skips programmatic focus, uses user-tap-only on a 56-px input with `WebkitAppearance:none`, and auto-saves on change. Typography contrast bump: `--c-text-dim` in `src/styles/tailwind.css` lifted from `#A8A498 -> #8A857A` (light) and `#4A4640 -> #6A655C` (dark) to hit ~4.5:1 contrast on warm-paper and warm-ink backgrounds. Avatar primitive gains `loading="eager" decoding="async"` so slow-loading R2 profile pictures don't flash the initial-badge fallback. All 28 ?v= strings bumped 20260418y -> 20260418z. React bundle `window.SortedReact.version` bumped `0.9.0 -> 0.9.1`.

PR-3.8 shipped: title edits are now inline only. The full-screen TITLE PropertySheet mount has been deleted from `srtd-next/src/flows/pcs/PCS.jsx` (and `TITLE_FIELD_CONFIG` removed). Tap the post detail h1 (gated by `canEdit`) to flip into a transparent inline `<input>` with `border:1px solid var(--c-terracotta)`; Enter or blur commits, Escape discards silently, empty/unchanged values exit without a patch. Commit goes through the existing `useOptimisticPatch` hook; success triggers `reseedOgPreview(post.post_id)` manually because the hook does not reseed OG. Vanilla `actions/pcs.js _pcsTitleEdit` (1156-1187) is untouched. `PropertySheet.jsx` / `FullScreenEditor.jsx` are unchanged — every other property still routes through them. React bundle `window.SortedReact.version` bumped `0.9.19 -> 0.9.20`.
